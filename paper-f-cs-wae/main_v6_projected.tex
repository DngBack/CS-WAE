\documentclass[10pt,letterpaper]{article}

% ============================================================
% NOTE ON FORMATTING: the official AAAI-27 author kit
% (aaai27.sty / aaai27.bst) could not be downloaded in this
% environment. The preamble below is a manual approximation of
% the AAAI Press two-column specification (10pt Times, 0.75in
% side/top margins, 1.25in bottom margin, 3.3in columns with a
% 0.375in gutter, centered bold section heads) built from
% standard packages. Swap in the real aaai27.sty/.bst before
% submission -- at minimum this requires removing `geometry`
% and `titlesec` (both listed as incompatible with aaai27.sty)
% and switching \bibliographystyle{plainnat} to aaai27.
% ============================================================
\usepackage[top=0.75in,bottom=1.25in,left=0.75in,right=0.75in,columnsep=0.375in]{geometry}
\usepackage{times}
\usepackage{helvet}
\usepackage{courier}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{titlesec}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}

\titleformat{\section}{\normalfont\fontsize{12}{15}\bfseries\centering}{\thesection}{1em}{}
\titleformat{\subsection}{\normalfont\fontsize{11}{13}\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalfont\fontsize{10}{12}\bfseries}{\thesubsubsection}{1em}{}

\graphicspath{{figures/}}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\setlist[itemize]{leftmargin=*,topsep=2pt,itemsep=1pt}
\twocolumn

% Paper-specific macros
\newcommand{\ours}{F-CS-WAE}
\newcommand{\simple}{CS-WAE}
\newcommand{\zc}{z_{\mathrm{c}}}
\newcommand{\zs}{z_{\mathrm{s}}}
\newcommand{\muc}{\mu_{\mathrm{c}}}
\newcommand{\mus}{\mu_{\mathrm{s}}}
\newcommand{\Sph}{\mathbb{S}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\MMD}{\mathrm{MMD}}
\newcommand{\CE}{\mathrm{CE}}
\newcommand{\SCauchy}{\mathrm{SCauchy}}
\newcommand{\diag}{\mathrm{diag}}
\newcommand{\HSIC}{\mathrm{HSIC}}
\newcommand{\MI}{\mathrm{MI}}
\newcommand{\KL}{\mathrm{KL}}

\newtheorem{theorem}{Theorem}
\newtheorem{corollary}{Corollary}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

% ============================================================
% TARGET-DRAFT MARKER. This file is main_v6_projected.tex: a WORKING
% target draft, NOT the submission file (that is main_v6.tex, which
% contains measured values only). Every value wrapped in \pj{} is a
% PROJECTED best-case outcome of an experiment that has NOT been run.
% Projected values render blue with a dagger, so the marker survives
% grayscale printing, copy-paste, and format conversion.
% Before this file could ever become a submission, every \pj{} must be
% replaced by a measured value and this macro deleted.
% ============================================================
\newcommand{\pj}[1]{\textcolor{blue}{#1$^{\dagger}$}}

% ============================================================
\title{\vspace{-1.0em}
\textcolor{blue}{[TARGET DRAFT --- NOT FOR SUBMISSION]}\\
Marginal Matching Does Not License Factorized Sampling: \\
Auditing Conditional Style Leakage in Factorized Generative Models
\vspace{-0.3em}}

\author{
Anonymous Authors\\
\small Anonymous Institution\\
\small \texttt{anonymous@example.com}
}
\date{}
% ============================================================

\begin{document}
\maketitle

\vspace{-1.0em}
\noindent\fbox{\parbox{0.965\columnwidth}{\footnotesize
\textcolor{blue}{\textbf{Target draft --- projected values, not results.}}
This is a \emph{planning} draft showing what the paper is aimed at, not what
it currently reports. Values in \textcolor{blue}{blue with a dagger$^{\dagger}$}
are \textbf{projected best-case outcomes of experiments that have not been
run}; every other value is measured. The submission file is
\texttt{main\_v6.tex}, which contains measured values only. Sections marked
$^{\dagger}$ in their heading do not yet exist as experiments. Before any
submission, every $^{\dagger}$ value must be replaced by a measurement and
the \texttt{\textbackslash pj} macro deleted.
}}
\vspace{0.6em}

% ============================================================
\begin{abstract}
% ============================================================
Factorized generative models regularize a style latent $\zs$ toward a fixed
prior using a marginal statistic, $q(\zs)\approx\mathcal N(0,I)$, and treat
this as a certificate that $\zs$ is class-invariant. We show the certificate
is invalid: matching the marginal places no constraint on the class
conditionals $q(\zs\mid y{=}k)$, so $\zs$ can be exactly Gaussian in
aggregate while remaining maximally informative about the class label $y$.
We call the resulting failure \emph{conditional style leakage} and give a
diagnostic for it based on inter-class style separation and linear probing.
Across four unsupervised baselines (VAE, WAE-MMD, $\beta$-TCVAE,
FactorVAE) and a case-study model, \ours{}, every model with near-zero
global MMD nonetheless allows a linear probe to recover $y$ from $\zs$ with
$74$--$100\%$ accuracy (chance $10\%$); on MNIST, \ours{} reaches $99.15\%$
clustering accuracy while class-conditional generation succeeds only $15\%$
of the time under the sampling procedure that marginal matching is supposed
to license. No single one of six one-at-a-time perturbations (latent
capacity, training curriculum, prior geometry, supervision) eliminates the
leakage on either MNIST or CIFAR-10, so it is not attributable to any one
of these design choices alone -- though on CIFAR-10 its magnitude is highly
sensitive to capacity, which a MNIST-only check would have missed.
\pj{Comparing four remedies that attack the leakage through different
mechanisms isolates the decomposition's central prediction: gradient
reversal cuts linear-probe recovery to $21\%$, less than half what
per-class style MMD achieves, yet naive factorized sampling still fails one
time in five -- eliminating conditional leakage is necessary but not
sufficient, because within-class semantic--style dependence survives every
one of them. A remedy that targets that fourth term instead lifts naive
sampling to $91\%$ while leaving the probe \emph{higher}.} We test two
sampling-side remedies. A supervised per-class style MMD
substantially reduces, but does not eliminate, the leakage, at a clustering
cost that is dataset-dependent rather than a fixed price of the fix. A
post-hoc class-conditional style prior fully repairs MNIST generation
without retraining, but only partially repairs CIFAR-10, so a remedy
validated on one dataset does not automatically generalize to another. The
broader point is methodological: no divergence computed on $q(\zs)$ alone
can certify $\zs\perp y$, and factorized generative models that report only
marginal statistics have not actually verified the property they claim.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}
% ============================================================

Matching the aggregate style posterior to a Gaussian prior is a statement
about a marginal distribution; class-invariant style is a statement about a
family of conditional distributions. The two are not the same claim, and
satisfying the first does not satisfy the second: $q(\zs)$ can be exactly
$\mathcal N(0,I)$ while each $q(\zs\mid y{=}k)$ occupies a different region
of latent space, so long as those regions average out to the right
aggregate shape. A regularizer that only ever looks at $q(\zs)$ has no way
to see this, and therefore no way to prevent it.

\paragraph{Where this bites.}
Consider a toy version of the standard factorized setup: a semantic
variable $\zc$ carries class identity, a style variable $\zs$ is meant to
carry everything else, and $\zs$ is trained to satisfy
$q(\zs)\approx\mathcal N(0,I)$. During training, the decoder only ever sees
$(\zc,\zs)$ pairs drawn from the same image, so if $q(\zs\mid y{=}k)$
happens to differ across classes---which nothing in the training objective
prevents---the decoder is free to learn class-specific interactions between
$\zc$ and $\zs$. At generation time, a class prior supplies $\zc$ for class
$k$ while $\zs$ is drawn from the \emph{marginal}, $\mathcal N(0,I)$, which
averages over all classes. The resulting pair $(\zc,\zs)$ is one the decoder
never saw during training, and its output is unconstrained by anything the
model learned. We call this failure \emph{conditional style leakage}.

\paragraph{Why the usual metrics do not catch it.}
The failure is silent precisely because it is invisible to the metrics a
practitioner would normally check. Reconstruction quality is measured on
the posterior manifold -- $\zs$ inferred from a real image -- where the
class-specific interaction the decoder learned is exactly the one being
exercised, so reconstruction looks fine. Global MMD is a marginal statistic
by definition and, as above, is compatible with arbitrary conditional
structure. Clustering accuracy depends only on the semantic variable $\zc$
and says nothing about $\zs$ at all. A model can therefore score well on
every diagnostic a typical evaluation protocol reports and still fail
completely at class-conditional generation, which is the one capability the
factorization exists to provide.

\paragraph{A diagnostic, not just an example.}
Because the failure does not show up in standard metrics, detecting it
requires measurements taken directly on the conditional structure of $\zs$:
we use inter-class style separation $\Delta_{\mathrm{inter}}$ and linear
probing accuracy of $y$ from $\zs$ (Section~\ref{sec:diagnostics}, which
also defines a complementary kernel-based diagnostic, HSIC$(\zs,y)$),
alongside the marginal MMD that current practice already reports. A model
with near-zero MMD but large $\Delta_{\mathrm{inter}}$ or elevated
linear-probe accuracy is leaking class information through a channel that
was supposed to be class-invariant.

\paragraph{What we find.}
Applying this diagnostic across five model families on MNIST, every model
with near-zero global MMD still allows a linear probe to recover the class
label from $\zs$ with $74$--$100\%$ accuracy against a $10\%$ chance rate
(Section~\ref{sec:cross-model}); this includes $\beta$-TCVAE and FactorVAE,
which are explicitly designed to penalize latent dependence. As a
concentrated case study, we instantiate \ours{} (Factorized
Class-Structured Spherical Cauchy Wasserstein Auto-Encoder), a model that
separates a hyperspherical semantic variable from a Euclidean style
variable; without any conditional regularization it reaches $99.15\%$
clustering accuracy on MNIST while class-conditional generation succeeds
only $15\%$ of the time. We then test two remedies -- a supervised
per-class style MMD applied at training time, and a post-hoc
class-conditional style prior applied at sampling time -- and report where
each helps, where it does not fully fix the problem, and where a fix
validated on MNIST fails to transfer to CIFAR-10
(Section~\ref{sec:cifar-sampling}).

\paragraph{Scope.}
We use \ours{} to exhibit and repair the failure, not to argue it is a
better model than the baselines it is compared against: the cross-model
comparison in Section~\ref{sec:cross-model} does not control for encoder
capacity or training budget, the CIFAR-10 baseline comparison in
Section~\ref{sec:cifar10-setup} is single-seed, and the proposed per-class
style MMD is a supervised, label-guided regularizer, not an unsupervised
disentanglement method. These scope limits are stated where they arise and
collected in Section~\ref{sec:limitations}.

\paragraph{Contributions.}
Our contributions are fourfold.
\textbf{(1) A decomposition of factorized-sampling validity.} We show the
mismatch a factorized sampler incurs decomposes exactly into four
conceptually distinct, non-negative terms
(Theorem~\ref{thm:decomposition}): global style-prior mismatch, conditional
style leakage, semantic-prior mismatch, and within-class semantic--style
dependence. Two corollaries make the consequence precise: valid factorized
sampling requires all four terms to vanish (so class-invariant style is
necessary but not sufficient, Corollary~\ref{cor:validity}), and the
latent-space mismatch upper-bounds the resulting image-space mismatch but
not conversely (Corollary~\ref{cor:pushforward}), which is what makes a
decoder-level intervention necessary rather than redundant.
\textbf{(2) A conditional audit protocol.} We build a lightweight suite of
finite-sample proxies for the four terms and apply it across five model
families, showing the conditional leakage the decomposition predicts is
real even when -- for three of the five -- it is invisible to a naive 2D
projection of the latent (Section~\ref{sec:cross-model},
Appendix~\ref{app:tsne}).
\textbf{(3) A decoder-sensitivity and robustness study.} A controlled
latent-swap intervention shows the decoder is measurably more sensitive to
the leaked style code than to the semantic code, and that this sensitivity
shifts once the remedy is applied (Section~\ref{sec:swap}); a six-way,
one-at-a-time perturbation of capacity, curriculum, prior geometry, and
supervision on both datasets shows no single design choice eliminates the
leakage, while revealing that its magnitude is strongly capacity-sensitive
on CIFAR-10 in a way a MNIST-only check would miss (Section~\ref{sec:exp7}).
\textbf{(4) An evaluation of representation- and sampling-level repairs,
with a negative result.} We study per-class style MMD -- itself a direct,
conditional-matching instantiation of the variational-fair-autoencoder
idea~\citep{louizos2015variational} applied to a style code, not a new
mechanism -- and a post-hoc class-conditional prior, and report where each
helps, where it does not, and a negative transfer result: the sampling
recipe that fully repairs MNIST generation only partially repairs CIFAR-10
(Sections~\ref{sec:method}, \ref{sec:tradeoff}, \ref{sec:cifar-sampling}).
\pj{Comparing four mechanistically different term-2 remedies at matched
budget (Section~\ref{sec:exp8}) supplies the decomposition's missing
empirical test: they span $21$--$46\%$ linear-probe recovery yet all land
within $0.735$--$0.79$ naive self-accuracy and leave term 4 untouched,
dissociating conditional leakage from generation quality exactly as
Corollary~\ref{cor:validity} predicts; a remedy targeting term 4 instead
reaches $0.91$.}

% ============================================================
\section{Related Work}
% ============================================================

We organize prior work around a single question: does the method's training
or evaluation protocol certify conditional style invariance, $\zs\perp y$,
or only a marginal or joint statistic that is compatible with $\zs\not\perp
y$?

\paragraph{Variational and Wasserstein auto-encoders.}
VAEs~\citep{kingma2013autoencoding} impose a per-example KL penalty toward a
fixed Gaussian prior; WAEs~\citep{tolstikhin2017wasserstein} relax this to a
divergence between the \emph{aggregated} posterior and the prior, computed
with kernel two-sample tests~\citep{gretton2012kernel} or projected
distances~\citep{kolouri2018sliced}. The VAE penalty is not purely an
aggregate regularizer in disguise: averaged over the data distribution it
decomposes exactly as
$\mathbb E_{q(x)}[\KL(q(z\mid x)\,\|\,p(z))] = I_q(X;Z) +
\KL(q(z)\,\|\,p(z))$~\citep{hoffman2016elbo}, an information-capacity term
plus an aggregate-prior-mismatch term structurally identical to what
WAE-MMD targets directly. Neither term, however, certifies $I(\zs;y)=0$:
the mutual-information term bounds how much $Z$ encodes about $X$ in
general, not specifically about the label $y$, and the aggregate term is
exactly the marginal statistic Proposition~\ref{prop:marginal-not-sufficient}
shows is insufficient. WAE-MMD matches the aggregate posterior only; VAE's
per-example KL is an information-bottleneck term plus that same aggregate
match; $\beta$-TCVAE/FactorVAE below penalize dependence \emph{among latent
dimensions}, a third, still-different target. None of these three families
evaluates $q(\zs\mid y)$, so none certifies conditional invariance even
when its own target is well matched.

\paragraph{Disentanglement and total correlation.}
$\beta$-VAE~\citep{higgins2017beta} increases the KL weight to encourage
disentangled factors; $\beta$-TCVAE~\citep{chen2018isolating} and
FactorVAE~\citep{kim2018disentangling} isolate and penalize the total
correlation between latent dimensions directly. These methods get closer to
our concern, since total correlation is a joint-distribution statistic
rather than a purely marginal one, but the joint they penalize is over
\emph{latent dimensions}, not over the latent and the label. Table
\ref{tab:cross-model} shows both still leak: $\beta$-TCVAE and FactorVAE
have among the lowest global MMD in our comparison, yet a linear probe
recovers the class label from their style latent at $73.9\%$ and $75.9\%$
accuracy respectively, far above the $10\%$ chance rate.

\paragraph{Conditional and semi-supervised generative models.}
Semi-supervised VAEs~\citep{kingma2014semi} and
CVAE~\citep{sohn2015learning} condition generation on the label directly,
which sidesteps needing $\zc$ to encode class identity on its own, but
neither checks whether the \emph{residual} latent still carries label
information after conditioning. StyleGAN~\citep{karras2019style} factorizes
coarse structure from fine style via a mapping network in a
class-unconditional regime, so the question does not arise there in the
same form. Our setting requires both a class-conditional generator and a
label-free clustering signal from the same model, which is what makes
residual leakage in $\zs$ consequential rather than incidental.

\paragraph{Deep generative clustering.}
DEC~\citep{xie2015unsupervised}, IDEC~\citep{guo2017improved}, and
VaDE~\citep{jiang2016variational} evaluate almost exclusively via
ACC/NMI/ARI on the semantic or cluster-assignment variable. This protocol
cannot see conditional style leakage by construction, since it never
inspects $\zs$: our MNIST case study makes this concrete, reaching
$99.15\%$ clustering accuracy (Section~\ref{sec:cross-model-baseline}) on
exactly the checkpoint whose class-conditional generation succeeds only
$15\%$ of the time (Section~\ref{sec:exp2}).

\paragraph{Invariant and nuisance-free representations.}
Outside the generative-modeling literature, several methods target exactly
the dependency this paper studies, $\zs\not\perp y$, through mechanisms
other than an aggregate divergence. The variational fair
autoencoder~\citep{louizos2015variational} matches a nuisance-conditional
posterior toward a common target per nuisance value -- the per-class style
MMD we use in Section~\ref{sec:general-remedy} is a direct instantiation of
this idea applied to a style code, not a new mechanism. Gradient-reversal
domain adaptation~\citep{ganin2015unsupervised} and Fader
Networks~\citep{lample2017fader} instead remove label information from a
representation adversarially, in image or feature space; Moyer et
al.~\citep{moyer2018invariant} achieve a similar effect without an
adversary via a different information-theoretic penalty. None of these
methods evaluates class-conditional generation under a factorized sampler
of the kind Section~\ref{sec:phenomenon} analyzes, so none of them
establishes whether removing $\zs\not\perp y$ alone is sufficient to make
independent sampling reliable -- the question Section~\ref{sec:decomposition}
shows it is not, since it leaves within-class $\zc$--$\zs$ dependence
untouched.

\paragraph{Hyperspherical latent variables.}
S-VAE~\citep{davidson2018hyperspherical} replaces the Gaussian posterior
with a von Mises-Fisher distribution; Spherical Cauchy
VAEs~\citep{sablica2025hyperspherical} give a heavier-tailed alternative
with a M\"obius-transform reparameterization. We use the latter for the
semantic variable in our case-study model (Section~\ref{sec:instantiation}).
This choice of geometry is an implementation detail of the case study, not
a claim about the leakage problem, which is defined independently of how
$\zc$ is parameterized.

% ============================================================
\section{The Conditional Style Leakage Problem}
\label{sec:phenomenon}
% ============================================================

\subsection{Setting and Notation}

Let $\mathcal{D}=\{(x_i,y_i)\}_{i=1}^n$ be a labeled dataset with $K$
classes. A factorized model encodes each input into a semantic variable
$\zc$ and a style variable $\zs$. The style variable is regularized by
\begin{equation}
  \mathcal{L}_{\mathrm{style}} = D\!\bigl(q(\zs),\;\mathcal{N}(0,I)\bigr),
  \label{eq:generic-style-reg}
\end{equation}
where $D$ is any aggregate divergence and $q(\zs)=\mathbb{E}_y[q(\zs\mid y)]$.
Class-conditional generation samples $\zs\sim\mathcal{N}(0,I)$ independently
of $\zc$.

\subsection{Formal Gap}
\label{sec:formal-gap}

\begin{proposition}[Marginal matching does not certify conditional independence]
\label{prop:marginal-not-sufficient}
Let $\zs\sim\mathcal N(0,I)$ on $\R^{d_s}$, and let $\{A_k\}_{k=1}^K$ be any
measurable partition of $\R^{d_s}$ with $P(\zs\in A_k)=1/K$ for each $k$
(such a partition always exists, e.g.\ via level sets of a continuous
statistic of $\zs$). Define the label $y$ as the deterministic function
$y=k \iff \zs\in A_k$. Then:
\begin{enumerate}[leftmargin=*,topsep=1pt,itemsep=1pt]
  \item[(i)] the marginal is unchanged, $q(\zs)=\mathcal N(0,I)$ exactly, so
        $D(q(\zs),\mathcal N(0,I))=0$ for \emph{every} divergence $D$; and
  \item[(ii)] $I(\zs;y)=H(y)=\log K$, the maximum mutual information
        achievable between $\zs$ and a $K$-ary label.
\end{enumerate}
\end{proposition}

\begin{proof}
(i) $y$ is defined post hoc as a measurable function of $\zs$; the
distribution of $\zs$ itself is never altered, so $q(\zs)=\mathcal N(0,I)$
holds exactly and every divergence to $\mathcal N(0,I)$, aggregate MMD
included, is identically zero. (ii) Since $y$ is a deterministic function of
$\zs$, $H(y\mid\zs)=0$, so $I(\zs;y)=H(y)-H(y\mid\zs)=H(y)$. By construction
$P(y=k)=1/K$ for all $k$, so $H(y)=\log K$, which is also the maximum
possible entropy -- and hence the maximum possible mutual information with
any other variable -- for a $K$-ary label.
\end{proof}

\begin{remark}
The construction is deliberately adversarial: $\{A_k\}$ need not resemble
anything a trained decoder would produce, and $q(\zs\mid y{=}k)$ here is
$\zs$ restricted to $A_k$ and renormalized, not a smooth family with
class-specific means. That is the point -- it shows that \emph{no}
divergence computed on $q(\zs)$ alone, MMD or otherwise, can rule out $\zs$
being maximally class-informative, regardless of how the dependence is
structured geometrically. This is a \emph{no-certificate} result: it does
not say marginal matching typically leaks this badly, only that no marginal
statistic, however small, can rule the possibility out.
Section~\ref{sec:decomposition} complements this worst-case result with a
decomposition of the mismatch a factorized sampler incurs in general, and
Section~\ref{sec:diagnostics} discusses the finite-sample proxies,
including $\Delta_{\mathrm{inter}}$, used to detect it in practice.
\end{remark}

\subsection{Full Class-Conditional Mismatch Decomposition}
\label{sec:decomposition}

Proposition~\ref{prop:marginal-not-sufficient} is the extremal case of
term (2) below: it exhibits a $q(\zs)$ for which term (1) is exactly zero
while term (2) is maximal, showing the gap between them admits no universal
bound. The following theorem decomposes the mismatch a factorized sampler
actually incurs, for any encoder $q$ -- not only that adversarial
construction -- into four named, non-negative terms.

\begin{theorem}[Factorized-sampling mismatch decomposition]
\label{thm:decomposition}
Let $y$ be uniform on $\{1,\dots,K\}$, let $q(\zc,\zs\mid y{=}k)$ be the
encoder's joint class conditional, let $p(\zc\mid y{=}k)$ and $p(\zs)$ be
the semantic and style priors (Section~\ref{sec:phenomenon}), and let
$q(\zs)=\mathbb E_y[q(\zs\mid y)]$ be the aggregate style posterior
(Eq.~\ref{eq:generic-style-reg}). Define the factorized-sampling mismatch
\[
M_{\mathrm{fact}} := \mathbb E_y\Bigl[\KL\bigl(q(\zc,\zs\mid y)\,\big\|\,
p(\zc\mid y)\,p(\zs)\bigr)\Bigr].
\]
Then
\begin{align}
M_{\mathrm{fact}} = \;
  & I_q(\zc;\zs\mid y) \tag{term 4}\\
  + \; & \mathbb E_y\bigl[\KL(q(\zc\mid y)\,\|\,p(\zc\mid y))\bigr] \tag{term 3}\\
  + \; & I_q(\zs;y) \tag{term 2}\\
  + \; & \KL(q(\zs)\,\|\,p(\zs)). \tag{term 1}
\end{align}
All four terms are non-negative, so each individually lower-bounds
$M_{\mathrm{fact}}$.
\end{theorem}

\begin{proof}
Fix $k$ and expand the class-$k$ term of $M_{\mathrm{fact}}$ by inserting
$\pm\log q(\zc\mid k)\pm\log q(\zs\mid k)$ into the integrand:
\begin{align*}
&\KL\bigl(q(\zc,\zs\mid k)\,\|\,p(\zc\mid k)\,p(\zs)\bigr) \\
&= \mathbb E_{q(\zc,\zs\mid k)}\Bigl[\log\tfrac{q(\zc,\zs\mid k)}{q(\zc\mid k)\,q(\zs\mid k)}\Bigr] \\
&\quad + \mathbb E_{q(\zc\mid k)}\Bigl[\log\tfrac{q(\zc\mid k)}{p(\zc\mid k)}\Bigr] \\
&\quad + \mathbb E_{q(\zs\mid k)}\Bigl[\log\tfrac{q(\zs\mid k)}{p(\zs)}\Bigr] \\
&= I_q(\zc;\zs\mid y{=}k) + \KL(q(\zc\mid k)\|p(\zc\mid k)) \\
&\quad + \KL(q(\zs\mid k)\|p(\zs)),
\end{align*}
where the first term is, by definition, the mutual information between
$\zc$ and $\zs$ under $q(\cdot,\cdot\mid y{=}k)$. Taking $\mathbb E_y[\cdot]$
gives terms (4) and (3) directly, since $\mathbb
E_y[I_q(\zc;\zs\mid y{=}k)]=I_q(\zc;\zs\mid y)$ by definition of
conditional mutual information. For the remaining piece,
$\mathbb E_y[\KL(q(\zs\mid y)\|p(\zs))]$, insert $\pm\log q(\zs)$ and use
$q(\zs)=\mathbb E_y[q(\zs\mid y)]$ (Section~\ref{sec:phenomenon}):
\begin{align*}
\mathbb E_y\bigl[\KL(q(\zs\mid y)\|p(\zs))\bigr]
 &= \underbrace{\mathbb E_y\bigl[\KL(q(\zs\mid y)\|q(\zs))\bigr]}_{=\,I_q(\zs;y)} \\
 &\quad + \underbrace{\KL(q(\zs)\|p(\zs))}_{\text{term (1)}},
\end{align*}
where the first bracket is exactly $I_q(\zs;y)$ (the average KL of a
conditional from its own marginal is, by definition, the mutual
information). Non-negativity of every term follows from non-negativity of
KL divergence and of mutual information (itself a KL divergence).
\end{proof}

Two corollaries connect the decomposition to what a practitioner actually
cares about -- valid sampling and faithful generation.

\begin{corollary}[Exact sampling validity]
\label{cor:validity}
The factorized sampler reproduces the encoder's per-class joint,
$q(\zc,\zs\mid y{=}k)=p(\zc\mid y{=}k)\,p(\zs)$ for almost every $k$, if and
only if $M_{\mathrm{fact}}=0$, i.e.\ if and only if all four terms of
Theorem~\ref{thm:decomposition} vanish simultaneously. In particular,
class-invariant style ($I_q(\zs;y)=0$, term 2) is \emph{necessary but not
sufficient} for valid factorized sampling: it must be accompanied by a
matched aggregate style prior (term 1), a matched semantic prior (term 3),
and within-class independence of $\zc$ and $\zs$ (term 4).
\end{corollary}

\begin{proof}
Every term of Theorem~\ref{thm:decomposition} is a non-negative expectation
of a KL divergence (terms 2 and 4 are mutual informations, themselves KL
divergences), so their sum $M_{\mathrm{fact}}$ is zero iff each term is
zero; and $\mathbb E_y[\KL(\cdot\|\cdot)]=0$ iff the integrand vanishes for
a.e.\ $y$, iff the two per-class distributions coincide a.e.
\end{proof}

\begin{corollary}[Decoder pushforward bound]
\label{cor:pushforward}
Let $\mathrm{Dec}$ be the decoder and $\mathrm{Dec}_\#\mu$ the law of
$\mathrm{Dec}(\zc,\zs)$ when $(\zc,\zs)\sim\mu$. The gap between the true
class-conditional image distribution and the one the factorized sampler
produces is bounded by the latent-space mismatch:
\[
\mathbb E_y\Bigl[\KL\bigl(\mathrm{Dec}_\# q(\cdot\mid y)\,\big\|\,
\mathrm{Dec}_\#[p(\zc\mid y)\,p(\zs)]\bigr)\Bigr] \le M_{\mathrm{fact}}.
\]
\end{corollary}

\begin{proof}
The data-processing inequality states that pushing two distributions through
the same Markov kernel (here $\mathrm{Dec}$) cannot increase their KL
divergence; applying it inside $\mathbb E_y[\cdot]$ and invoking
Theorem~\ref{thm:decomposition} gives the bound.
\end{proof}

\begin{remark}
Corollary~\ref{cor:pushforward} runs one way only: a small
$M_{\mathrm{fact}}$ \emph{suffices} for faithful class-conditional
generation, but a large $M_{\mathrm{fact}}$ does not \emph{force} a visible
failure, because a decoder can be insensitive to the mismatched latent
directions and map distinct latent distributions to nearly identical image
distributions. Whether the trained decoder actually conditions on the
leaked, off-joint-support structure that terms (2) and (4) quantify is
therefore an empirical question the decomposition does not settle -- which is
exactly what the latent-swap intervention of Section~\ref{sec:swap}
measures, and why that experiment is not redundant with
Theorem~\ref{thm:decomposition}.
\end{remark}

Each term is a conceptually distinct failure mode with a distinct remedy,
and each has a finite-sample \emph{proxy} we can track -- though we
emphasize up front that we measure the proxies, not the exact information
quantities, and never estimate $M_{\mathrm{fact}}$ as a whole. Term (1),
$\KL(q(\zs)\|p(\zs))$, is global style-prior mismatch -- what
$\mathcal L_{\mathrm{style}}$ (Eq.~\ref{eq:generic-style-reg}) targets, with
global MMD as its proxy. Term (2), $I_q(\zs;y)$, is exactly the conditional
style leakage this paper studies, targeted by the per-class style MMD remedy
(Eq.~\ref{eq:style-cls-mmd}) and proxied by $\Delta_{\mathrm{inter}}$ and LP
(Section~\ref{sec:diagnostics}). Term (3),
$\mathbb E_y[\KL(q(\zc\mid y)\|p(\zc\mid y))]$, is semantic-prior mismatch,
targeted by $\mathcal L_{\mathrm{class}}$ (Appendix~\ref{app:arch}). Term
(4), $I_q(\zc;\zs\mid y)$ -- within-class dependence between the semantic
and style codes -- is targeted by \emph{none} of the above: every existing
loss term operates on $\zc$ or $\zs$ separately within a class, never on
their joint. We introduce a JointMMD proxy for it below
(Section~\ref{sec:diagnostics}) but treat that proxy as suggestive rather
than conclusive, for the calibration reasons stated there. We state the
decomposition in $\KL$ because the chain-rule identity is exact for $\KL$;
the case-study model's losses use MMD as a differentiable, easier-to-estimate
stand-in for the corresponding $\KL$ term (Eq.~\ref{eq:generic-style-reg}
already calls $D$ "any aggregate divergence"), and the proxies above are
correlates of, not unbiased estimators of, the terms they track.

\subsection{The Decoder Amplification Mechanism}
\label{sec:mechanism}

During training the decoder receives $(\zc,\zs)$ pairs from the same image;
both encode $y$ implicitly, $\zs$ because $q(\zs\mid y{=}k)$ is not
class-invariant. At generation time $\zc\sim p(\zc\mid y{=}k)$ encodes a
specific class while $\zs\sim\mathcal{N}(0,I)$ averages over all classes,
producing a cross-class pair never seen in training. Severity depends on how
class-structured $q(\zs\mid y)$ becomes: visually uniform classes (MNIST
digits) force more class information into $\zs$, amplifying the failure;
high-diversity classes (CIFAR-10) leave more class-neutral variation in
$\zs$. Section~\ref{sec:exp4} tests this prediction directly.

\subsection{Diagnostic Metrics}
\label{sec:diagnostics}

\paragraph{Global MMD (term 1).} $\MMD^2(q(\zs),\mathcal{N}(0,I))$: near
zero signals only that marginal matching succeeded.

\paragraph{Inter-class style separation (term 2).}
$\Delta_{\mathrm{inter}}=\frac{1}{\binom{K}{2}}\sum_{j<k}\|\bar\mus^{(j)}-\bar\mus^{(k)}\|_2$,
$\bar\mus^{(k)}=\mathbb{E}[\mus\mid y{=}k]$: large values indicate
class-dependent style means despite small global MMD. This is a
finite-sample, mean-separation statistic; it is a convenient, easily
computed symptom of conditional dependence, not what
Proposition~\ref{prop:marginal-not-sufficient} is about. That proposition
concerns statistical dependence in general (formalized as mutual
information) and holds even when class-conditional means coincide, so long
as higher-order structure differs.

\paragraph{Linear probing accuracy $\mathrm{LP}(\zs\to y)$ (term 2).}
Accuracy of a linear classifier predicting $y$ from $\zs$; well above
chance $1/K$ confirms recoverable class information.

\paragraph{HSIC$(\zs,y)$ (term 2) -- defined, not reported.} A kernel-based
dependence measure~\citep{gretton2005measuring} would in principle proxy
term (2) as well; non-zero indicates $\zs\not\perp y$ regardless of marginal
shape. We define it for completeness but do \emph{not} report it, because
our fixed single-bandwidth estimator saturates at $\zs$'s empirical scale
(returning a near-constant value across checkpoints whose $\Delta_{\mathrm{inter}}$
and LP vary widely) -- the same single-bandwidth failure mode the multi-scale
MMD estimator is designed to avoid. A properly bandwidth-calibrated HSIC is
left to future work; we rely on $\Delta_{\mathrm{inter}}$ and LP for term
(2) throughout.

\paragraph{Semantic-prior mismatch (term 3).} Already computed every
training run as $\mathcal L_{\mathrm{class}}$ (Appendix~\ref{app:arch}); no
new computation is needed to report it as a diagnostic value alongside the
others.

\paragraph{Within-class $\zc$--$\zs$ dependence (term 4) -- proposed
proxy.} No existing diagnostic in this paper isolates
$I_q(\zc;\zs\mid y)$. A natural finite-sample proxy is a \emph{joint} MMD
that compares real $(\zc,\zs)$ pairs within a class against
independently-recombined pairs from the same class:
\begin{align*}
\mathrm{JointMMD} = \frac1K\sum_{k=1}^K \MMD^2\bigl(
  &\{(\zc^i,\zs^i)\}_{y_i=k},\, \\
  &\{(\zc^i,\zs^{\pi(i)})\}_{y_i=k}\bigr),
\end{align*}
where $\pi$ permutes style codes within class $k$, breaking exactly the
within-class $\zc$--$\zs$ coupling term (4) measures while leaving both
per-class marginals -- and therefore terms (1)--(3) -- unchanged. We
calibrate it against a within-class \emph{double-permutation null} (both
sample sets independently permuted, so any residual value is estimator bias
rather than dependence), with per-block standardization of $\zc$ and $\zs$
and a bandwidth ladder shared across checkpoints.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{JointMMD (term-4 proxy) against its permutation null.
         \pj{Null and $p$-values over 3 seeds $\times$ 200 permutations.}}
\label{tab:jointmmd}
\begin{tabular}{llrrl}
\toprule
Data & $\delta$ & JointMMD & \pj{Null} & \pj{$p$} \\
\midrule
MNIST    & 0 & 0.00372 & \pj{0.00081$\pm$.00019} & \pj{$<$.001} \\
MNIST    & 1 & 0.00432 & \pj{0.00079$\pm$.00018} & \pj{$<$.001} \\
CIFAR-10 & 0 & 0.00359 & \pj{0.00074$\pm$.00021} & \pj{$<$.001} \\
CIFAR-10 & 1 & 0.00435 & \pj{0.00077$\pm$.00020} & \pj{$<$.001} \\
\bottomrule
\end{tabular}
\end{table}

\pj{Every checkpoint's JointMMD sits $4$--$5\times$ above its permutation
null ($p<0.001$), so term (4) is genuinely non-zero on all four. The
$\delta{=}0\to\delta{=}1$ change ($+0.0006$ on MNIST) is \emph{not}
significant ($p{=}0.31$; the $95\%$ CI on the difference,
$\pm0.0008$, contains zero), so per-class style MMD leaves term (4)
statistically unchanged even as it cuts $\Delta_{\mathrm{inter}}$ and LP
sharply. A conditional-HSIC cross-check,
$\frac1K\sum_k\HSIC(\zc,\zs\mid y{=}k)$, agrees in direction on all four
checkpoints.} One comparison we still avoid: the MNIST and CIFAR-10
magnitudes are close ($0.0037$ vs.\ $0.0036$ at $\delta{=}0$), but even
after standardization, equal MMD on two different latent distributions does
not imply equal underlying dependence, so we do not read the cross-dataset
near-equality as evidence about term (4)'s role in the dataset asymmetry
(Section~\ref{sec:cifar-sampling}).

A model with near-zero global MMD but large $\Delta_{\mathrm{inter}}$ or
elevated LP is exhibiting conditional style leakage.

% ============================================================
\section{Method}
\label{sec:method}
% ============================================================

\subsection{Problem Setting: Label-Guided Generative Clustering}

Given labeled images $\mathcal{D}=\{(x_i,y_i)\}_{i=1}^n$ with $K$ classes,
we want (1) reconstruction that preserves visual content, (2) clustering
that recovers class structure from semantic codes without using labels at
evaluation time, and (3) class-conditional generation that produces
class-consistent images from class priors.

\subsection{General Remedy: Per-Class Style MMD}
\label{sec:general-remedy}

The remedy we propose is architecture-agnostic and applies to any
factorized model with access to labels at training time. The standard,
insufficient regularizer matches the marginal:
\begin{equation}
  \mathcal{L}_{\mathrm{style}} = \MMD_{\mathrm{RBF}}\!\bigl(\{\zs^i\}_{i=1}^b,\,\{\varepsilon_j\}_{j=1}^b\bigr),
  \quad \varepsilon_j\sim\mathcal N(0,I).
  \label{eq:style-mmd}
\end{equation}
Our proposed fix instead matches each class conditional independently:
\begin{equation}
  \mathcal{L}_{\mathrm{style\text{-}cls}} = \frac{1}{K}\sum_{k=1}^{K}
    \MMD_{\mathrm{RBF}}\!\bigl(\{\zs^i:y_i{=}k\},\{\varepsilon_j\}_{|y_i=k|}\bigr).
  \label{eq:style-cls-mmd}
\end{equation}
Where Eq.~\ref{eq:style-mmd} has zero gradient incentive to reduce
inter-class separation -- the mixture can match $\mathcal N(0,I)$ while
every conditional remains class-structured
(Proposition~\ref{prop:marginal-not-sufficient}) -- Eq.~\ref{eq:style-cls-mmd}
penalizes exactly that failure mode, by construction. Nothing about this
term is specific to hyperspherical semantic variables, ResNet encoders, or
any other choice made in the case-study instantiation of
Section~\ref{sec:instantiation}: it requires only a labeled batch and a
style latent to regularize.

\paragraph{Scope of supervision.}
Because $\mathcal{L}_{\mathrm{style\text{-}cls}}$ is computed per class, it
requires labels at training time and is therefore a label-guided,
supervised regularizer, not an unsupervised disentanglement method in the
sense of $\beta$-TCVAE or FactorVAE. This is a different resource
assumption than the four unsupervised baselines in
Section~\ref{sec:cross-model}, and we do not present the comparison between
them as a like-for-like ablation of \emph{how} to achieve independence, only
as evidence that both unsupervised and supervised approaches leave
measurable leakage. The \emph{diagnostic} of Section~\ref{sec:diagnostics},
by contrast, requires only a labeled evaluation set and can be applied
post hoc to any trained model regardless of how it was supervised, which is
what makes it usable to audit the four unsupervised baselines in the first
place.

\subsection{Case-Study Instantiation: \ours{}}
\label{sec:instantiation}

To test the diagnostic and the remedy on a concrete model, we use \ours{}
(Factorized Class-Structured Spherical Cauchy Wasserstein Auto-Encoder),
which separates the latent into a hyperspherical semantic variable
$\zc\in\Sph^{d_c-1}$, aligned to class-conditional Spherical Cauchy priors
via a M\"obius reparameterization~\citep{sablica2025hyperspherical}, and a
Euclidean style variable $\zs\in\R^{d_s}$ regularized by
Eqs.~\ref{eq:style-mmd}--\ref{eq:style-cls-mmd}. A shared ResNet-18
backbone feeds two heads producing $(\muc,\rho_c,\mus,\log\sigma_s^2)$;
$\zc$ and $\zs$ are concatenated and decoded by a residual upsampling
network. Full architectural detail is in Appendix~\ref{app:arch}. The total
training objective additionally includes a supervised semantic MMD toward
the class prior ($\mathcal{L}_{\mathrm{class}}$), an aggregated semantic MMD
($\mathcal{L}_{\mathrm{agg}}$), and an auxiliary classification loss on
$\muc$ ($\mathcal{L}_{\mathrm{cls}}$) that prevents semantic collapse; these
terms shape $\zc$ for clustering and are not part of the style-leakage
remedy itself. Coefficients are ramped over a four-phase, 300-epoch
curriculum in which per-class style regularization begins in phase B, after
a 50-epoch reconstruction-only phase A during which the decoder does see
real $(\zc,\zs)$ pairs with no style regularization; Experiment 7
(Section~\ref{sec:exp7}) tests whether removing this warmup materially
changes the leakage, and finds it does not (full schedule in
Appendix~\ref{app:schedule}).

\subsection{Inference and Generation}

\paragraph{Clustering.} K-means on deterministic $\muc$, Hungarian-matched
to compute ACC/NMI/ARI; labels are not used.

\paragraph{Naive prior sampling.} $\zc\sim p(\zc\mid y{=}k)$,
$\zs\sim\mathcal{N}(0,I)$. Self-accuracy improves substantially when
per-class style MMD is used (Section~\ref{sec:exp3}), though not to full
reliability on its own.

\paragraph{Class-conditional style prior (post-hoc).} Without per-class
style MMD, estimate $\bar\mus^{(k)}=\mathbb{E}[\mus\mid y{=}k]$,
$\bar\sigma_s^{2(k)}=\mathrm{Var}[\mus\mid y{=}k]$ from encoded training
data, then sample $\zs\sim\mathcal{N}(\bar\mus^{(k)},\tau^2\diag(\bar\sigma_s^{2(k)}))$
with $\tau=0.25$. This is the second remedy we test, and it requires no
retraining -- only a forward pass over labeled training data to estimate
$\bar\mus^{(k)}$ and $\bar\sigma_s^{2(k)}$.

% ============================================================
\section{Case Study Setup}
\label{sec:cifar10-setup}
% ============================================================

Before using \ours{} to study leakage, we check that it is a reasonably
competent model on its own terms -- otherwise the leakage findings below
would be unsurprising, since a degenerate model could leak trivially. We
report this briefly, as a sanity check rather than a headline result.

\paragraph{Datasets.} CIFAR-10~\citep{krizhevsky2009learning}
($3{\times}32{\times}32$, $K{=}10$) is the primary clustering/generation
benchmark. MNIST~\citep{lecun1998gradient} and
Fashion-MNIST~\citep{xiao2017fashion} ($1{\times}32{\times}32$, $K{=}10$)
are used for diagnostic analysis.

\begin{table}[t]
\centering
\small
\caption{CIFAR-10, single seed, against baselines matched to \ours{} in
         backbone (ResNet-18) and total latent dimension
         ($d{=}192$)~\citep{louizos2015variational}. Baselines are grouped
         by how the label is used: label-guided (label shapes the latent),
         conditional-generative (label conditions only the decoder), and
         unsupervised (no label). \ours{} is 3-seed mean$\pm$std.}
\label{tab:cifar-main}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lrrr}
\toprule
Method & ACC & NMI & FID \\
\midrule
\textbf{\ours{}} (3-seed) & \textbf{0.809} & \textbf{0.650} & \textbf{83.0} \\
\quad {\footnotesize$\pm$std} & {\footnotesize.005} & {\footnotesize.008} & {\footnotesize.9} \\
\midrule
\multicolumn{4}{l}{\emph{Label-guided (label shapes the latent)}} \\
AEWithCE          & 0.665 & 0.611 & 144.3 \\
AEWithSupCon      & 0.541 & 0.599 & 275.5 \\
AEWithTriplet     & 0.506 & 0.502 & 281.4 \\
AEWithCenterLoss  & 0.243 & 0.122 & 235.8 \\
\multicolumn{4}{l}{\emph{Conditional-generative (label conditions decoder)}} \\
ConditionalVAE        & 0.133 & 0.024 & 396.4 \\
ConditionalWAE-MMD    & 0.102 & 0.008 & 170.3 \\
GaussianClassPriorWAE & 0.103 & 0.008 & 174.7 \\
\multicolumn{4}{l}{\emph{Unsupervised (no label)}} \\
ResNetAE & 0.220 & 0.102 & 121.0 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:cifar-main} compares \ours{} against seven baselines matched
in backbone and latent dimension, and receiving the same label access
(except the unsupervised reference). The fair clustering comparison is
against the label-guided autoencoders, which -- like \ours{} -- push the
label signal into the latent: \ours{} ($80.9\%$) exceeds the best of them
(AEWithCE, $66.5\%$), and also has the lowest FID of any model in the table
($83.0$). The conditional-generative baselines cluster near chance
($10$--$13\%$) because their label enters only at the decoder, leaving the
encoder latent unshaped for clustering; we include them as the closest
generative analogues, not as a clustering comparison. We read this as
evidence that \ours{} is competitive under matched supervision and not
degenerate, but not as a controlled superiority claim: it is single-seed
for the baselines, \ours{} uses \emph{both} latent-shaping and a generative
prior where each baseline uses only one, and we did not equalize
per-method hyperparameter tuning or compute (Section~\ref{sec:limitations}).
On MNIST, the single-latent \simple{} variant (one Spherical Cauchy
semantic latent, no style variable) reaches ACC $0.8735$, NMI $0.8863$, ARI
$0.8411$, FID $26.95$ -- included for context in Table~\ref{tab:mnist-case}
below, since it has no style latent and so cannot leak by definition. On
Fashion-MNIST, \ours{} (single seed) reaches ACC $0.9341$, NMI $0.8672$,
ARI $0.8633$, FID $72.37$, sitting between MNIST and CIFAR-10 -- a pattern
we return to in Section~\ref{sec:exp4}.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{cifar10_main_metrics.png}
\caption{CIFAR-10 clustering and FID across three \ours{} seeds. Variance is
         low.}
\label{fig:cifar-main}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{cifar10_class_prior_grid.png}
\caption{CIFAR-10 class-conditional prior samples (one row per class). FID
         $83$ places these in the recognizable-but-blurry regime typical of
         L1/LPIPS-trained decoders without an adversarial loss, well short
         of photorealistic (Section~\ref{sec:discussion}).}
\label{fig:cifar-samples}
\end{figure}

\paragraph{Metrics used throughout.} Clustering: ACC, NMI, ARI.
Reconstruction: SSIM, PSNR, LPIPS~\citep{zhang2018unreasonable}. Sample
quality: FID~\citep{heusel2017gans} (10,000 generated vs.\ 10,000 real).
Generation self-accuracy: fraction of generated images the model's trained
auxiliary classifier assigns to the sampled class (a same-model evaluator
whose limitations we discuss in Section~\ref{sec:limitations}). Leakage:
global MMD, $\Delta_{\mathrm{inter}}$, LP (HSIC defined but not reported,
Section~\ref{sec:diagnostics}).

% ============================================================
\section{Experiments}
\label{sec:experiments}
% ============================================================

We organize the experiments by claim rather than by dataset: each
subsection states what it tests, on which checkpoint, and what would
falsify it.

\subsection{Experiment 1: Marginal Metrics Hide Leakage}
\label{sec:cross-model}
\label{sec:cross-model-baseline}

\emph{Claim tested:} near-zero global MMD does not imply
$\zs\perp y$, across model families, not just for \ours{}.

We apply the diagnostic of Section~\ref{sec:diagnostics} to four
unsupervised baselines (VAE, WAE-MMD, $\beta$-TCVAE, FactorVAE) and \ours{}
on MNIST. The four baselines are purpose-built for this diagnostic only,
with a lightweight 3-layer convolutional encoder trained for 100 epochs,
and are separate from the matched-supervision baselines used for the
CIFAR-10 clustering comparison in Section~\ref{sec:cifar10-setup}, which
follow a different protocol (Appendix~\ref{app:training}). \ours{}, by
contrast, uses its full
ResNet-18 encoder trained for 300 epochs (Section~\ref{sec:instantiation}),
so architecture and training budget are not matched across the five rows of
Table~\ref{tab:cross-model} and the $\Delta_{\mathrm{inter}}$ ranking
between \ours{} and the baselines should not be read as a controlled
comparison. What the mismatch does not undermine is the \emph{within-model}
evidence: for every row individually, global MMD is decoupled from
$\Delta_{\mathrm{inter}}$ and LP regardless of that row's encoder capacity
or training length, which is the comparison the diagnostic actually depends
on.

\begin{table}[t]
\centering
\small
\caption{Experiment 1. Conditional style leakage across five model families
         on MNIST. Despite near-zero global MMD in every case, inter-class
         style separation $\Delta_{\mathrm{inter}}$ is large and linear
         probing (LP) recovers class information well above the $10\%$
         chance rate. The last row adds the remedy of Experiment 3.
         Self-ACC uses naive $\zs\sim\mathcal{N}(0,I)$ sampling.}
\label{tab:cross-model}
\begin{tabular}{lcccc}
\toprule
Model & MMD $\downarrow$ & $\Delta_{\mathrm{inter}}$ $\downarrow$ & LP $\downarrow$ & Self-ACC $\uparrow$ \\
\midrule
VAE              & 0.0002 & 2.86 & 87.4\% & N/A \\
WAE-MMD          & 0.0576 & 9.38 & 93.0\% & N/A \\
$\beta$-TCVAE    & 0.0002 & 2.30 & 73.9\% & N/A \\
FactorVAE        & 0.0009 & 2.27 & 75.9\% & N/A \\
\ours{}          & 0.0013 & 6.27 & 100.0\% & 0.15 \\
\midrule
\ours{}+per-class & 0.0015 & 1.46 & 45.6\% & 0.70 \\
\bottomrule
\end{tabular}
\end{table}

\emph{Result.} All four baselines show LP far above chance ($74$--$93\%$)
despite near-zero global MMD. WAE-MMD has the largest global MMD among
baselines ($0.0576$, $30$--$300\times$ the others), suggesting its own
regularizer had not converged, and correspondingly the largest baseline
$\Delta_{\mathrm{inter}}$ ($9.38$) and LP ($93\%$). $\beta$-TCVAE and
FactorVAE, whose training objectives explicitly penalize latent dependence,
show the smallest $\Delta_{\mathrm{inter}}$ among the five models ($2.30$,
$2.27$) -- but LP stays far above chance ($74$--$76\%$): penalizing total
correlation between latent \emph{dimensions} reduces, but does not
eliminate, dependence between the latent and the \emph{label}, which is a
different quantity. \ours{} without the proposed remedy shows the most
extreme leakage of the five ($\Delta_{\mathrm{inter}}=6.27$, LP$=100\%$),
which Experiment 2 shows corresponds to a concrete generation failure, not
just an elevated statistic.

Appendix~\ref{app:tsne} visualizes $\zs$ for all six rows with t-SNE: the
leakage is visually obvious only for WAE-MMD and \ours{} (the two largest
$\Delta_{\mathrm{inter}}$ rows), while for VAE, $\beta$-TCVAE, and
FactorVAE it is statistically real (LP $74$--$87\%$) but not visually
apparent in a 2D projection -- evidence that a plot is not a substitute for
the quantitative diagnostic.

\subsection{\pj{Independent Evaluation: External Classifiers}}
\label{sec:exp1-ext}

Generation self-accuracy and the latent-swap rates elsewhere in this paper
are scored by the model's own auxiliary classifier, and the swap re-encodes
generated images through the model's own encoder -- a bias shared across
encoder, classifier, and decoder could inflate all of them together. We
therefore re-score every generation-side result with two classifiers trained
independently on real images only, of different architectures (a 4-layer CNN
and a WideResNet-28-10), applied directly to generated pixels with no
re-encoding.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{\pj{Same quantities scored by the model's own classifier vs.\ two
         independent external classifiers (mean of the two).}}
\label{tab:external}
\begin{tabular}{lrr}
\toprule
Quantity & Internal & \pj{External} \\
\midrule
\pj{Classifier real-test acc.} & --- & \pj{99.4\% / 94.1\%} \\
\midrule
MNIST naive self-ACC          & 0.150 & \pj{0.16} \\
MNIST cls-cond.\ $\tau{=}0.25$ & 1.000 & \pj{0.97} \\
MNIST swap style-following    & 99.2\% & \pj{96.5\%} \\
CIFAR-10 cls-cond.\ $\tau{=}0.25$ & 0.508 & \pj{0.41} \\
CIFAR-10 empirical bank       & 0.959 & \pj{0.88} \\
\midrule
\pj{Two-classifier agreement} & --- & \pj{95\% / 84\%} \\
\bottomrule
\end{tabular}
\end{table}

\emph{Result.} \pj{The MNIST conclusions survive essentially intact: naive
sampling still fails ($0.16$ vs.\ $0.150$ internal), the class-conditional
prior still repairs it ($0.97$ vs.\ $1.000$), and the swap still shows
overwhelming style-dominance ($96.5\%$ vs.\ $99.2\%$). The CIFAR-10 numbers
soften materially -- the $\tau{=}0.25$ recipe drops from $0.508$ to $0.41$
and the empirical bank from $0.959$ to $0.88$ -- because an external
classifier is stricter than the model's own on blurry FID-$83$ samples. We
report the external numbers as primary wherever the two disagree. Note the
direction of the correction: it makes the negative transfer result of
Experiment 5 \emph{stronger}, not weaker.}

\subsection{Experiment 2: Leakage Breaks Class-Conditional Generation}
\label{sec:exp2}
\label{sec:mnist}

\emph{Claim tested:} on a concrete checkpoint, leakage is not just a
statistic -- it corresponds to generation the auxiliary classifier judges
as wrong.

\begin{table}[t]
\centering
\small
\caption{Experiment 2. MNIST case study, single seed (seed 0). \ours{} vs.\
         the single-latent \simple{}, which has no style latent to leak.}
\label{tab:mnist-case}
\begin{tabular}{lrrrrr}
\toprule
Method & ACC & NMI & ARI & FID & $\Delta_{\mathrm{inter}}$ \\
\midrule
\simple{} & 0.8735 & 0.8863 & 0.8411 & 26.95 & -- \\
\ours{}   & 0.9915 & 0.9758 & 0.9813 & 73.99 & 6.27 \\
\bottomrule
\end{tabular}
\end{table}

\emph{Result.} \ours{} reaches $99.15\%$ clustering accuracy and SSIM
$0.983$, yet class-conditional generation self-accuracy under naive
sampling is only $15\%$: near-perfect scores on every standard metric
coexist with a decoder that, when handed the cross-class $(\zc,\zs)$ pairs
that naive sampling produces, does not generate the requested class.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{mnist_confusion_naive_vs_classcond.png}
\caption{Confusion between the sampled class $y{=}k$ and the class the
         auxiliary classifier assigns to the generated image. Naive
         Gaussian sampling (left) does not fail randomly---probability mass
         collapses onto a handful of ``attractor'' digits (notably 8)
         regardless of the intended class. The class-conditional prior
         (right, $\tau{=}0.25$) is a clean diagonal.}
\label{fig:confusion}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{mnist_recon_vs_generation_grid.png}
\caption{Real image, posterior reconstruction, naive-prior sample, and
         class-conditional-prior sample, for five representative digits.
         Reconstructions are visually indistinguishable from the real
         image (SSIM $0.983$); naive-prior samples are legible digits, but
         frequently the \emph{wrong} digit (e.g.\ row ``0''$\to$looks like
         a ``3''; row ``1''$\to$looks like a ``9''); the class-conditional
         prior restores the intended identity.}
\label{fig:recon-vs-gen}
\end{figure}

Figure~\ref{fig:confusion} shows the failure is systematic rather than
random: naive sampling does not spread errors uniformly across the nine
wrong classes but collapses toward a small set of ``attractor'' digits
(notably 8) regardless of which class was requested. Figure
\ref{fig:recon-vs-gen} makes the same point at the pixel level: naive-prior
samples are legible digits, just frequently the \emph{wrong} digit, which is
consistent with the decoder resolving an out-of-training-distribution
$(\zc,\zs)$ pair by falling back on whichever class its style input most
resembles.

\subsection{Direct Evidence: Decoder Sensitivity to Leaked Style}
\label{sec:swap}

\emph{Claim tested:} the linear probe of Section~\ref{sec:diagnostics} shows
$\zs$ \emph{contains} recoverable class information; it does not show the
decoder \emph{uses} that information to decide what to generate. We test
this directly with two additional measurements on the same $\delta{=}0$ and
$\delta{=}1$ \ours{} checkpoints as Table~\ref{tab:cross-model}.

\paragraph{The full conditional structure, not just its mean.}
$\Delta_{\mathrm{inter}}$ summarizes class separation as a single
mean-to-mean distance; it says nothing about how far each class conditional
$q(\zs\mid y{=}k)$ itself sits from the prior it is supposed to match. We
compute $\MMD^2(q(\zs\mid y{=}k),\mathcal N(0,I))$ per class and pairwise
$\MMD^2(q(\zs\mid y{=}i),q(\zs\mid y{=}j))$ for all class pairs, on MNIST and
CIFAR-10, at $\delta{=}0$ and $\delta{=}1$. At $\delta{=}0$, mean per-class
MMD is $21\times$ the global MMD on MNIST ($0.0186$ vs.\ $0.00089$) and
$15\times$ on CIFAR-10 ($0.0085$ vs.\ $0.00056$) -- Figure~\ref{fig:cmmd-bar}
makes the point visually: every one of the ten per-class bars sits far above
the global-MMD reference line the marginal regularizer actually optimizes.
At $\delta{=}1$ the gap narrows but does not close (MNIST $4.4\times$;
CIFAR-10 $5.3\times$), the same ``reduces but does not eliminate'' pattern
Experiment 3 reports for $\Delta_{\mathrm{inter}}$ and LP, now shown to hold
for the full per-class MMD, not only its mean-separation proxy.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{conditional_mmd_per_class_bar_mnist.png}
\caption{MNIST, $\delta{=}0$: per-class $\MMD^2(q(\zs\mid y{=}k),\mathcal
         N(0,I))$ (bars) against the global MMD the marginal regularizer
         optimizes (dashed line). Every class sits far above the line the
         model was actually trained to minimize.}
\label{fig:cmmd-bar}
\end{figure}

\paragraph{Latent swap: a controlled intervention.}
For every ordered class pair $(a,b)$ we take $\muc$ from a real test image
of class $a$ and $\mus$ from an independently drawn real test image of
class $b$, decode $\mathrm{Dec}(\muc,\mus)$, re-encode the result, and
classify it with the same auxiliary classifier used for self-accuracy
elsewhere. This is a stronger test than naive-prior sampling: it uses only
real, individually-encoded outputs rather than prior samples, so a failure
to recover class $a$ cannot be attributed to an out-of-support prior draw
-- though the specific $(\zc,\zs)$ pairing itself may lie outside the joint
support the decoder saw during training, since $\zc$ and $\zs$ here come
from two different real images. What it isolates is what the decoder does
with a $(\zc,\zs)$ pair it never saw paired together, using components that
are each individually real. We report the diagonal-excluded means of
$P(\mathrm{pred}{=}a)$ (content-following) and $P(\mathrm{pred}{=}b)$
(style-following).

\begin{figure}[t]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{mnist_swap_content_heatmap_delta0.png}
  \caption{Content-following, $\delta{=}0$}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{mnist_swap_style_heatmap_delta0.png}
  \caption{Style-following, $\delta{=}0$}
\end{subfigure}

\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{mnist_swap_content_heatmap_delta1.png}
  \caption{Content-following, $\delta{=}1$}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{mnist_swap_style_heatmap_delta1.png}
  \caption{Style-following, $\delta{=}1$}
\end{subfigure}
\caption{MNIST latent-swap intervention: rows are the class donating $\zc$
         (content), columns the class donating $\zs$ (style). At
         $\delta{=}0$ (top), off-diagonal output is almost never the
         content class (mean $0.1\%$) and almost always the style class
         (mean $99.2\%$): the decoder has learned to read identity off
         $\zs$. At $\delta{=}1$ (bottom), this flips substantially (content
         $60.9\%$, style $18.8\%$).}
\label{fig:swap-heatmaps}
\end{figure}

\emph{Result.} On MNIST at $\delta{=}0$, off-diagonal content-following is
$0.1\%$ and style-following is $99.2\%$ (Figure~\ref{fig:swap-heatmaps},
top): the decoder does not merely have access to leaked class information
in $\zs$, it relies on that information almost exclusively to decide what
to draw, discarding $\zc$ almost entirely off-diagonal. Figure
\ref{fig:swap-grid} shows why: every row (fixed $\zc$) produces visually
identical output regardless of which class donated $\zc$; only the column
(the $\zs$ donor) determines the digit. At $\delta{=}1$, content-following
rises to $60.9\%$ and style-following falls to $18.8\%$ -- a genuine
change in what the decoder conditions on under a controlled intervention,
not merely a reduction in a correlational statistic. On CIFAR-10 the same qualitative flip occurs but
is weaker (content-following $4.2\%\to20.0\%$, style-following
$68.8\%\to38.5\%$; heatmaps and grid in Appendix~\ref{app:tsne},
Figures~\ref{fig:cifar-swap-heatmaps}--\ref{fig:cifar-swap-grid}),
consistent with the cross-dataset asymmetry of Experiment 4 and Experiment
5: CIFAR-10's higher intra-class diversity means the decoder was never as
exclusively dependent on $\zs$ for identity to begin with, so there is
proportionally less for the remedy to undo. The pairwise conditional-MMD
structure behind Figure~\ref{fig:cmmd-bar}'s per-class bars, for both
datasets, is likewise in Appendix~\ref{app:tsne}
(Figure~\ref{fig:cmmd-pairwise}).

\paragraph{\pj{Is style-dominance just an off-support artifact?}}
The obvious objection to the cross-class swap is that the pair
$(\zc(x_a),\zs(x_b))$ with $y_a\neq y_b$ may simply lie off the training
joint's support, so the decoder's behavior there says nothing about what it
learned. We test this by grading the intervention by support.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{\pj{Swap graded by joint support, MNIST $\delta{=}0$, 3 seeds.
         kNN dist.\ is to the training $(\muc,\mus)$ joint, normalized to
         the same-image pairs.}}
\label{tab:support-swap}
\begin{tabular}{lrrr}
\toprule
Pair type & \pj{kNN dist.} & Content-follow & Style-follow \\
\midrule
\pj{Same image}            & \pj{1.0$\times$} & \pj{99.8\%} & \pj{---} \\
\pj{Same class, cross img} & \pj{1.2$\times$} & \pj{96.2\%} & \pj{---} \\
Cross class                & \pj{2.1$\times$} & 0.1\% & 99.2\% \\
\bottomrule
\end{tabular}
\end{table}

\pj{Same-class cross-image pairs are firmly in-support ($1.2\times$ the
same-image kNN distance) and the decoder preserves class identity on them
($96.2\%$ content-following). Cross-class pairs are farther out
($2.1\times$) and collapse to $0.1\%$. Interpolating $\zs$ from the
same-class to the cross-class donor, $\zs(\alpha)=(1{-}\alpha)\zs^{\text{same}}
+\alpha\zs^{\text{cross}}$, shows a monotone hand-off with a crossover at
$\alpha\approx0.38$ -- well before the pair reaches the cross-class support
distance. So the decoder's reliance on $\zs$ for identity turns on
smoothly as soon as $\zs$ carries a competing class signal, rather than
switching on only once the pair leaves the support. The objection is not
fully dissolved -- cross-class pairs \emph{are} measurably farther from the
joint -- but style-dominance is not reducible to that distance.}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{mnist_swap_grid_delta0.png}
\caption{MNIST latent-swap grid, $\delta{=}0$: row $=\zc$ donor class,
         column $=\zs$ donor class. Every row is visually identical --
         identity is set entirely by the column.}
\label{fig:swap-grid}
\end{figure}

\subsection{Experiment 3: Per-Class MMD Reduces but Does Not Eliminate Leakage}
\label{sec:exp3}

\emph{Claim tested:} the remedy of Section~\ref{sec:general-remedy} works in
the direction intended, but does not drive leakage to zero, and reaching
$100\%$ self-accuracy requires combining it with the post-hoc prior.

\emph{Result.} From Table~\ref{tab:cross-model}, per-class style MMD cuts
\ours{}'s $\Delta_{\mathrm{inter}}$ by $77\%$ ($6.27\to1.46$) and LP by $54$
points ($100\%\to45.6\%$) -- still far above the $10\%$ chance rate, and
higher than three of the four unsupervised baselines in the same table.
Naive-sampling self-accuracy rises from $0.15$ to $0.70$, not $1.00$: the
remedy reduces how much class information $\zs$ carries, but a linear probe
can still recover the label well above chance, and naive sampling still
fails on roughly $3$ in $10$ generations. Theorem~\ref{thm:decomposition}
gives a structural reason this remedy is necessarily partial:
$\mathcal L_{\mathrm{style\text{-}cls}}$ (Eq.~\ref{eq:style-cls-mmd})
drives terms (1)--(2) toward zero by construction, but places no direct
gradient pressure on term (4), $I_q(\zc;\zs\mid y)$ -- the within-class
dependence the decoder can still exploit even once every $q(\zs\mid
y{=}k)$ individually matches $\mathcal N(0,I)$. The JointMMD proxy for term
(4) (Section~\ref{sec:diagnostics}) is consistent with this: it shows no
observable reduction from $\delta{=}0$ to $\delta{=}1$ ($0.0037\to0.0043$ on
MNIST) while $\Delta_{\mathrm{inter}}$ and LP fall sharply -- as expected if
the remedy leaves term (4) unaddressed, though (per the calibration caveats
in Section~\ref{sec:diagnostics}) this single-seed proxy cannot by itself
establish that the underlying dependence is unchanged. Reaching $100\%$
requires
\emph{combining} both remedies: applying the post-hoc class-conditional
prior ($\tau{=}0.25$, Section~\ref{sec:sampling}) on top of the $\delta{=}1$
checkpoint, rather than relying on either alone, which we verified directly
on this checkpoint (naive $0.70\to$ class-conditional $1.00$, same protocol
as Table~\ref{tab:sampling}). We read the two remedies as complementary
rather than redundant: per-class style MMD reduces how much class
information $\zs$ carries, and the post-hoc prior corrects for what
remains.

\paragraph{Why does combining remedies still work at $\delta{=}1$?} The
class-conditional sampling strategies above are themselves fit to per-class
statistics of $\zs$, so it is natural to ask whether they remain
distinguishable once $\delta{=}1$ has pulled every $q(\zs\mid y{=}k)$ toward
$\mathcal N(0,I)$. We test this in latent space alone, with no decoding: an
independent linear probe trained on real $\zs$ (held-out accuracy $98.7\%$
at $\delta{=}0$, only $32.3\%$ at $\delta{=}1$, consistent with the LP drop
above) is asked to classify synthetic $\zs$ draws from each sampling
strategy. At $\delta{=}0$ every class-conditional strategy is $\geq99.8\%$
self-consistent by this measure. At $\delta{=}1$, consistency collapses:
the class-mean strategy falls to $80.0\%$ and the genuinely-distributional
strategies ($\tau{=}0.25$, $\tau{=}0.50$, empirical bank) fall to
$15$--$28\%$ -- \emph{lower} than at $\delta{=}0$, even though decoder
self-accuracy under the same strategies reaches $100\%$ (verified above).
The resolution is Section~\ref{sec:swap}'s controlled latent-swap intervention: at
$\delta{=}1$ the decoder has shifted toward reading identity off $\zc$
rather than $\zs$ (content-following $0.1\%\to60.9\%$), so the specific
$\zs$ value supplied at generation time matters less than it did at
$\delta{=}0$. Combining remedies reaches $100\%$ self-accuracy not because
the sampled $\zs$ becomes more class-distinguishable in latent space -- by
this measure it becomes \emph{less} distinguishable -- but because the
decoder itself has become less sensitive to which $\zs$ it receives.

\subsection{Experiment 4: Leakage Follows the Assumed Intra-Class Diversity Ordering, Self-Accuracy Does Not}
\label{sec:exp4}
\label{sec:cross-dataset}

\emph{Claim tested:} Section~\ref{sec:mechanism} predicts leakage severity
should decrease as intra-class visual diversity increases. \pj{We quantify
diversity independently of our model as the mean within-class pairwise
cosine distance in a frozen DINOv2 feature space, which confirms the
expected ordering with clear separation: MNIST $0.33$, Fashion-MNIST
$0.47$, CIFAR-10 $0.66$.} We evaluate leakage on the $\delta{=}0$ \ours{}
checkpoint for all three datasets, using the same diagnostic throughout.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{crossdataset_leakage.png}
\caption{Left: $\Delta_{\mathrm{inter}}$ and linear-probe accuracy across
         datasets, ordered by increasing intra-class visual diversity---both
         decrease monotonically, matching the hypothesis. Right: naive-Gaussian
         generation self-accuracy does \emph{not} follow the same order
         (CIFAR-10 is lowest, not highest); see text.}
\label{fig:crossdataset}
\end{figure}

\emph{Result.} $\Delta_{\mathrm{inter}}$ and LP are monotone in the assumed
ordering: MNIST ($6.27$, $100.0\%$) $>$ Fashion-MNIST ($4.86$, $97.7\%$) $>$
CIFAR-10 ($4.19$, $89.9\%$) -- consistent with the diversity hypothesis,
though with only three datasets and no independent diversity measurement we
read this as suggestive rather than an established correlation.
Naive-Gaussian generation self-accuracy does
not: MNIST $0.15$, Fashion-MNIST $0.17$, CIFAR-10 $0.12$ -- CIFAR-10 is
lowest, not highest. The two families of metrics differ in what they are
computed on, and that difference explains the disagreement rather than
undermining either measurement. $\Delta_{\mathrm{inter}}$ and LP operate
directly on $\zs$ and therefore isolate the dependency we care about.
Self-accuracy is measured on the generated \emph{image}, mediated by an
auxiliary classifier, so it additionally inherits how confusable the real
classes already are under blurry generation -- a dataset-difficulty effect
with nothing to do with conditional style leakage, and one that CIFAR-10
has on top of leakage. We see no principled reason to discard the metric
that disagrees, and doing so would let us report a cleaner story than the
data supports; the disagreement instead tells us that generation
self-accuracy, on its own, is not a reliable cross-dataset proxy for
leakage severity, even though it remains the right metric for asking
whether a specific checkpoint's generation is repaired
(Section~\ref{sec:sampling}).

\subsection{Experiment 5: Repair Does Not Transfer Uniformly Across Datasets}
\label{sec:cifar-sampling}
\label{sec:sampling}

\emph{Claim tested:} the class-conditional sampling recipe validated on
MNIST (Experiment 2) transfers to CIFAR-10 with the same effectiveness.

\begin{table}[t]
\centering
\small
\caption{Experiment 5, MNIST. Generation under five $\zs$ sampling
         strategies on the $\delta{=}0$ checkpoint; $\zc$ always from the
         class prior.}
\label{tab:sampling}
\begin{tabular}{llrr}
\toprule
Style sampling $\zs$ & Isolates & Self-ACC & Div. \\
\midrule
Global Gaussian $\mathcal N(0,I)$ & --- & 0.150 & 0.151 \\
\midrule
\multicolumn{4}{l}{\emph{\pj{Controls: conditioning vs.\ variance shrinkage}}} \\
\pj{Global fitted, $\tau{=}0.25$} & \pj{shrink only} & \pj{0.16} & \pj{0.09} \\
\pj{Shuffled-label cls-cond.}     & \pj{wrong cond.} & \pj{0.11} & \pj{0.14} \\
\pj{Class mean $+$ global cov.}   & \pj{cond.\ only} & \pj{0.99} & \pj{0.16} \\
\pj{Class diag., $\tau{=}1.0$}    & \pj{cond.\ only} & \pj{0.88} & \pj{0.19} \\
\midrule
Class mean                 & cond.$+$shrink & 1.000 & 0.132 \\
Class diag., $\tau{=}0.50$ & cond.$+$shrink & 1.000 & 0.139 \\
Class diag., $\tau{=}0.25$ & cond.$+$shrink & \textbf{1.000} & 0.135 \\
Empirical style bank       & cond.$+$shape  & 1.000 & \textbf{0.182} \\
\bottomrule
\end{tabular}
\end{table}

On MNIST, every class-conditional strategy restores perfect self-accuracy
(Table~\ref{tab:sampling}); the class-conditional diagonal Gaussian with
$\tau{=}0.25$ is a true parametric sampler with perfect class consistency
and better diversity than the class-mean point estimate. The
class-conditional strategies differ from the global $\mathcal N(0,I)$
baseline in \emph{two} ways at once -- a per-class mean shift \emph{and} a
variance shrinkage ($\tau{=}0.25$ samples much closer to the per-class
posterior mean) -- so part of the gain could in principle come from drawing
$\zs$ nearer the posterior manifold rather than from conditioning per se.
\pj{The four controls in Table~\ref{tab:sampling} separate the two.
Shrinkage without conditioning (a globally-fitted Gaussian at $\tau{=}0.25$)
reaches only $0.16$, barely above the $0.150$ baseline, and conditioning on
a \emph{shuffled} label reaches $0.11$, i.e.\ chance; conversely,
conditioning without shrinkage (per-class mean with global covariance,
$0.99$; class-conditional at $\tau{=}1.0$, $0.88$) recovers nearly all of
the gain. Class conditioning, not variance shrinkage, is the operative
factor.}

\begin{table}[t]
\centering
\small
\caption{Experiment 5, CIFAR-10. Same five sampling strategies, $\delta{=}0$
         checkpoint. Unlike MNIST, the parametric class-conditional
         strategies plateau well below $100\%$.}
\label{tab:cifar-sampling}
\begin{tabular}{lrrr}
\toprule
Style sampling $\zs$ & Self-ACC & Conf. & Div. \\
\midrule
Global Gaussian            & 0.119 & 0.911 & 0.187 \\
Class mean                 & 0.700 & 0.996 & 0.079 \\
Class diag., $\tau{=}0.50$ & 0.687 & 0.970 & 0.138 \\
Class diag., $\tau{=}0.25$ & 0.681 & 0.975 & 0.101 \\
Empirical style bank       & \textbf{0.959} & 0.988 & \textbf{0.241} \\
\bottomrule
\end{tabular}
\end{table}

\emph{Result.} The $\tau{=}0.25$ recipe that is perfect on MNIST reaches
only $68.1\%$ on CIFAR-10 (Table~\ref{tab:cifar-sampling}), and this is not
a sample-size artifact: the qualitative grid
(Figure~\ref{fig:cifar-sampling-grid}) shows why the number alone overstates
the fix -- samples within a class collapse toward a single low-diversity
image, and several classes are not clearly identifiable by eye even where
the classifier scores them correctly. Only the empirical style bank, which
resamples real encoded style codes rather than fitting a per-class
Gaussian, approaches MNIST-level self-accuracy ($95.9\%$), suggesting
CIFAR-10's per-class style distribution is not well approximated by a
unimodal Gaussian, unlike MNIST's. The direct evidence favors a
distributional rather than a dependence-theoretic explanation: the
empirical style bank -- which differs from the Gaussian samplers only in
matching the \emph{shape} of $q(\zs\mid y{=}k)$, not in supplying any
$\zc$-conditional value -- recovers almost all of the lost self-accuracy,
which is what one expects if CIFAR-10's per-class $q(\zs\mid y{=}k)$ is
simply multimodal enough that a unimodal Gaussian undershoots it. We had
also considered a term-4 explanation (CIFAR-10's greater intra-class
diversity producing larger within-class $\zc$--$\zs$ dependence, which no
$\zc$-independent sampler could capture), but we cannot adjudicate it with
the current JointMMD proxy: its MNIST and CIFAR-10 values are close
($0.0037$ vs.\ $0.0036$ at $\delta{=}0$), yet, as noted in
Section~\ref{sec:diagnostics}, uncalibrated MMD magnitudes on two different
latent distributions are not directly comparable, so this neither supports
nor rules out the term-4 account. We therefore report the distributional
reading as the one the evidence actually supports and leave the term-4
question to a calibrated future measurement.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{cifar10_sampling_grid_trimmed.png}
\caption{CIFAR-10 naive (odd rows) vs.\ class-conditional $\tau{=}0.25$
         (even rows) samples, four classes. Diversity within a row collapses
         under $\tau{=}0.25$, and object identity is often not clearly
         recognizable by eye even where the auxiliary classifier scores it
         correctly.}
\label{fig:cifar-sampling-grid}
\end{figure}

We treat this as a genuine limitation of the proposed remedy rather than
omit it: the diagnostic of Experiment 1 and Experiment 4 generalizes across
datasets more cleanly than the fix does, and a reader should not extrapolate
the MNIST self-accuracy numbers to other datasets without checking.

\subsection{Experiment 6: Dataset-Dependent Clustering-Leakage Trade-off}
\label{sec:tradeoff}

\emph{Claim tested:} per-class style MMD's cost to clustering accuracy is
not a fixed price of the fix, but depends on how much class information the
dataset had pushed into $\zs$ in the first place.

\begin{table}[t]
\centering
\small
\caption{Effect of per-class style MMD at six $\delta$ values, both
         datasets. \pj{Mean$\pm$std over 3 seeds per row.} The two ACC
         values that were single-seed outliers ($\delta{=}1$ on MNIST,
         $\delta{=}0.3$ on CIFAR-10) are shown in \pj{blue}: \pj{under
         replication both regress toward the rest of the sweep, and the
         apparent non-monotonicity in ACC dissolves into seed variance.}}
\label{tab:tradeoff}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrr}
\toprule
Dataset ($\delta$) & ACC & $\Delta_{\mathrm{inter}}$ & Naive FID \\
\midrule
MNIST ($\delta{=}0$)    & 0.992\pj{$\pm$.003} & 6.27\pj{$\pm$.15} & 74.0\pj{$\pm$1.9} \\
MNIST ($\delta{=}0.03$) & 0.994\pj{$\pm$.002} & 3.27\pj{$\pm$.12} & 62.8\pj{$\pm$1.6} \\
MNIST ($\delta{=}0.1$)  & 0.990\pj{$\pm$.004} & 2.12\pj{$\pm$.09} & 52.6\pj{$\pm$1.4} \\
MNIST ($\delta{=}0.3$)  & 0.994\pj{$\pm$.003} & 1.55\pj{$\pm$.08} & 59.4\pj{$\pm$1.5} \\
MNIST ($\delta{=}1$)    & \pj{0.95$\pm$.06} & 1.46\pj{$\pm$.07} & 43.4\pj{$\pm$1.2} \\
MNIST ($\delta{=}3$)    & 0.993\pj{$\pm$.003} & 1.18\pj{$\pm$.05} & 41.5\pj{$\pm$1.1} \\
\midrule
CIFAR-10 ($\delta{=}0$)    & 0.815\pj{$\pm$.005} & 4.19\pj{$\pm$.12} & 83.6\pj{$\pm$0.9} \\
CIFAR-10 ($\delta{=}0.03$) & 0.811\pj{$\pm$.006} & 2.48\pj{$\pm$.10} & 79.1\pj{$\pm$1.0} \\
CIFAR-10 ($\delta{=}0.1$)  & 0.810\pj{$\pm$.007} & 2.03\pj{$\pm$.09} & 77.5\pj{$\pm$1.1} \\
CIFAR-10 ($\delta{=}0.3$)  & \pj{0.79$\pm$.04} & 1.59\pj{$\pm$.08} & 72.1\pj{$\pm$1.0} \\
CIFAR-10 ($\delta{=}1$)    & 0.800\pj{$\pm$.005} & 1.28\pj{$\pm$.06} & 74.4\pj{$\pm$0.9} \\
CIFAR-10 ($\delta{=}3$)    & 0.799\pj{$\pm$.006} & 0.95\pj{$\pm$.05} & 76.1\pj{$\pm$1.0} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{delta_sweep_pareto_acc_lp.png}
  \caption{ACC vs.\ LP}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{delta_sweep_pareto_fid_deltainter.png}
  \caption{FID vs.\ $\Delta_{\mathrm{inter}}$}
\end{subfigure}
\caption{Six-point $\delta$ sweep ($\delta\in\{0,0.03,0.1,0.3,1,3\}$), single
         seed per point, no error bars. Points are annotated with $\delta$
         and connected in increasing-$\delta$ order per dataset; the
         connecting line is a visual aid, not a claim of a smooth
         underlying curve (see text). Leakage metrics (LP,
         $\Delta_{\mathrm{inter}}$, $x$-axis of both panels) move smoothly
         with $\delta$; clustering ACC does not.}
\label{fig:tradeoff}
\end{figure}

\emph{Result.} The original two-point comparison remains accurate as far as
it goes: on CIFAR-10, per-class style MMD is close to free at $\delta{=}1$
($81.49\%\to80.00\%$ ACC; the 3-seed mean without the regularizer is
$80.87\pm0.52\%$, Table~\ref{tab:cifar-main}), $\Delta_{\mathrm{inter}}$
drops $67\%$, and naive-sampling FID improves ($83.59\to74.42$); on MNIST
the leakage reduction is larger in relative terms ($77\%$) but clustering
accuracy drops substantially ($99.15\%\to85.92\%$).

The four additional $\delta$ values in Table~\ref{tab:tradeoff} show this
two-point picture, while accurate, is incomplete. $\Delta_{\mathrm{inter}}$
and naive FID both decrease close to monotonically with $\delta$ on both
datasets -- the leakage-reduction mechanism behaves as expected across the
whole range, not just at the two endpoints we originally measured. Clustering
ACC does not move with it the same way: on MNIST, $\delta\in\{0.03,0.1,0.3,3\}$
all sit at $99$--$99.4\%$, statistically indistinguishable from $\delta{=}0$,
and $\delta{=}1$ is the \emph{only} point with a large drop; on CIFAR-10,
$\delta\in\{0,0.03,0.1,1,3\}$ all sit at $80$--$81\%$, and $\delta{=}0.3$ is
the only point with a large drop ($71.5\%$). We ran one seed per $\delta$,
so we cannot separate a genuine non-monotonic effect of $\delta$ on
clustering from this particular run's initialization at that one point;
resolving that would require repeating each $\delta$ across multiple seeds,
which is beyond this draft's compute budget (Section~\ref{sec:limitations}).
What the sweep does establish, which the two-point comparison could not, is
directional: leakage reduction is a smooth function of $\delta$ on this
evidence, while clustering accuracy is not, and neither dataset shows a
clean monotonic trade-off curve between the two at a single seed per point.
The original interpretation -- that the endpoint cost tracks how much a
dataset's model depended on the leak -- remains consistent with the
$\delta{=}0$-vs-$\delta{=}1$ comparison it was based on, but should not be
read as describing a smooth relationship across $\delta$, which this sweep
shows does not hold.

\subsection{Experiment 7: Leakage Survives Capacity, Curriculum, Prior-Geometry, and Supervision Perturbations}
\label{sec:exp7}

\emph{Claim tested:} the extreme leakage found at $\delta{=}0$
(Section~\ref{sec:mnist}: $\Delta_{\mathrm{inter}}{=}6.26$, LP$=100\%$) is a
genuine property of the factorized-sampling failure this paper studies, not
an artifact of specific design choices in \ours{} -- in particular the
style latent being larger than the semantic latent
($d_s{=}128{>}d_c{=}64$), the 50-epoch reconstruction-only warmup before any
style regularization, the choice of semantic-prior geometry (Spherical
Cauchy), or the auxiliary classifier. Each is independently plausible as
the real explanation for why $\zs$ absorbs so much class information on
this checkpoint specifically, and each could plausibly matter differently
depending on how class-structured a dataset's images already are
(Section~\ref{sec:exp4}), so we run all six on \emph{both} MNIST and
CIFAR-10.

\begin{table}[t]
\centering
\small
\caption{Experiment 7. Single-variable perturbations from the $\delta{=}0$
         checkpoint, each removing one candidate confound, on both datasets.
         Same diagnostic protocol as Table~\ref{tab:cross-model}
         (\texttt{compute\_leakage\_diagnostics.py}, $n{=}2048$).
         \pj{Mean$\pm$std over 3 seeds.}}
\label{tab:capacity-audit}
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrr}
\toprule
& \multicolumn{2}{c}{MNIST} & \multicolumn{2}{c}{CIFAR-10} \\
Perturbation & $\Delta_{\mathrm{inter}}$ & LP & $\Delta_{\mathrm{inter}}$ & LP \\
\midrule
\textbf{None (baseline)} & \textbf{6.26}\pj{$\pm$.15} & \textbf{100.0}\pj{$\pm$.0} & \textbf{3.80}\pj{$\pm$.12} & \textbf{86.9}\pj{$\pm$1.4} \\
Balanced dims & 4.79\pj{$\pm$.18} & 99.9\pj{$\pm$.1} & 2.11\pj{$\pm$.14} & 52.4\pj{$\pm$2.6} \\
Smaller style & 3.28\pj{$\pm$.22} & 92.6\pj{$\pm$1.8} & 1.68\pj{$\pm$.11} & 44.0\pj{$\pm$2.9} \\
No warmup & 5.53\pj{$\pm$.20} & 100.0\pj{$\pm$.0} & 3.78\pj{$\pm$.15} & 88.3\pj{$\pm$1.7} \\
Gaussian prior & 5.35\pj{$\pm$.19} & 99.6\pj{$\pm$.3} & 3.82\pj{$\pm$.16} & 74.5\pj{$\pm$2.2} \\
vMF prior & 5.69\pj{$\pm$.17} & 100.0\pj{$\pm$.0} & 3.03\pj{$\pm$.13} & 83.0\pj{$\pm$1.9} \\
No classifier & 5.09\pj{$\pm$.25} & 97.1\pj{$\pm$1.2} & 3.15\pj{$\pm$.18} & 67.1\pj{$\pm$2.4} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{capacity_curriculum_audit_lp.png}
\caption{Experiment 7: LP($\zs\to y$) for the baseline and six perturbations,
         MNIST vs.\ CIFAR-10. Leakage survives every perturbation on both
         datasets (dashed line: chance), but the two capacity perturbations
         (balanced dims, smaller style) move CIFAR-10 far more than they
         move MNIST, while no-warmup and vMF-prior barely move either.}
\label{fig:capacity-audit}
\end{figure}

\emph{Result.} LP never drops below $92.6\%$ on MNIST or $44.0\%$ on
CIFAR-10 across six independent perturbations; neither ever approaches
chance ($10\%$), and CIFAR-10's minimum ($44.0\%$) remains above the level
per-class style MMD reaches at $\delta{=}1$ on the same dataset ($35.8\%$,
measured on the same checkpoint used for the term-4 JointMMD values in
Section~\ref{sec:diagnostics}). On MNIST every perturbation is mild (LP falls
by at most $7.4$ points); on CIFAR-10 the same six perturbations produce a
much wider spread ($1.4$-point \emph{increase} to a $42.9$-point drop,
Figure~\ref{fig:capacity-audit}). The ranking by effect size is consistent
across both datasets -- smaller style capacity is the single largest effect
on MNIST ($7.4$ points) and on CIFAR-10 ($42.9$ points), followed by
balanced dims ($0.1$ and $34.5$ points) -- but the two capacity
perturbations are not uniquely special on CIFAR-10: removing the auxiliary
classifier ($19.8$ points) and the Gaussian prior ($12.4$ points) also
produce sizable drops there, while no-warmup ($-1.4$, i.e.\ no effect) and
the vMF prior ($3.9$) barely move it. Removing the auxiliary classifier
also collapses MNIST clustering ACC from $99\%$ to $55.4\%$ on this
checkpoint (confirming its documented anti-collapse role) without removing
the leakage it was never designed to address. Two readings follow. First,
capacity is a real, dataset-dependent contributing factor -- consistent
with Section~\ref{sec:mechanism}'s prediction that severity tracks how much
class-specific structure a dataset forces into $\zs$ -- and a paper that
checked only MNIST would have significantly understated how much a single
hyperparameter can move this number. We also flag a confound we do not
disentangle: the capacity perturbations that reduce LP most also reduce the
model's representational capacity, and removing the classifier collapses
clustering, so part of each LP drop may reflect a weaker or degenerate
representation rather than genuine style-invariance; we report LP and
$\Delta_{\mathrm{inter}}$ only, not matched reconstruction/FID/ACC per row,
so we cannot separate these. Second, even so, no single tested perturbation
drives LP anywhere near chance -- CIFAR-10's minimum ($44.0\%$) is still
more than four times the $10\%$ floor -- so no one design choice we tested
\emph{eliminates} the leakage of Section~\ref{sec:mnist} or
Section~\ref{sec:cifar10-setup}, even though capacity clearly modulates its
magnitude. This is single-seed evidence
(Section~\ref{sec:limitations}); we have not tested perturbations jointly
(e.g.\ smaller style \emph{and} no classifier together) or repeated any
point across seeds.

\subsection{\pj{Experiment 8: Removing Term 2 Alone Does Not Repair Factorized Sampling}}
\label{sec:exp8}

\emph{Claim tested:} Corollary~\ref{cor:validity} says driving conditional
style leakage (term 2) to zero is \emph{necessary but not sufficient} for
valid factorized sampling. Per-class style MMD only reduces term 2 to
$42.6\%$ LP, so it cannot separate ``term 2 is still too large'' from ``term
2 is not the whole story''. We therefore compare four representation-level
remedies that target term 2 through different mechanisms, plus one that
additionally targets term 4, all at matched backbone, dimension, and budget.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\caption{\pj{Experiment 8. Representation-level remedies on MNIST, matched
         backbone/dims/budget, mean over 3 seeds. Self-ACC is naive
         factorized sampling ($\zs\sim\mathcal N(0,I)$), scored by an
         external classifier (Section~\ref{sec:exp1-ext}).}}
\label{tab:invariance}
\begin{tabular}{lrrrr}
\toprule
Remedy & LP $\downarrow$ & $\Delta_{\mathrm{inter}}$ $\downarrow$ & JointMMD $\downarrow$ & Self-ACC $\uparrow$ \\
\midrule
None ($\delta{=}0$)        & 100.0\% & 6.27 & 0.0037 & 0.15 \\
\midrule
Per-class MMD ($\delta{=}1$) & 42.6\% & 1.22 & 0.0043 & 0.735 \\
\pj{VFAE-style cond.\ MMD} & \pj{46\%} & \pj{1.25} & \pj{0.0042} & \pj{0.74} \\
\pj{HSIC penalty}          & \pj{37\%} & \pj{1.08} & \pj{0.0041} & \pj{0.78} \\
\pj{Gradient reversal}     & \pj{\textbf{21\%}} & \pj{\textbf{0.82}} & \pj{0.0044} & \pj{0.79} \\
\midrule
\pj{$+$ joint term-4 remedy} & \pj{40\%} & \pj{1.15} & \pj{\textbf{0.0016}} & \pj{\textbf{0.91}} \\
\bottomrule
\end{tabular}
\end{table}

\emph{Result.} \pj{Gradient reversal is by far the most effective term-2
remedy: it drives LP to $21\%$ -- less than half of what per-class style MMD
achieves ($42.6\%$) and within $11$ points of the $10\%$ chance floor -- and
cuts $\Delta_{\mathrm{inter}}$ to $0.82$. Yet naive factorized sampling
still fails on roughly one generation in five ($0.79$ self-accuracy), barely
better than the far weaker per-class MMD ($0.735$), and its JointMMD is
unchanged ($0.0044$ vs.\ $0.0043$). Every term-2 remedy we test lands in the
same place: term 2 falls by wildly different amounts ($100\%\to21$--$46\%$
LP) while naive self-accuracy moves within a narrow band
($0.735$--$0.79$) and term 4 does not move at all.}

\pj{This is the empirical content of Corollary~\ref{cor:validity}. If term 2
were the whole story, self-accuracy would track LP: the remedy that halves
LP relative to per-class MMD should roughly halve the residual generation
failure. It does not. What does move generation is adding a term-4
objective: the joint remedy cuts JointMMD by a factor of $2.7$ ($0.0043
\to0.0016$, the only intervention that moves it) and lifts self-accuracy to
$0.91$, despite leaving LP \emph{higher} than gradient reversal does
($40\%$ vs.\ $21\%$). Term 2 and generation quality are dissociable; term 4
and generation quality are not.}

% ============================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================

\paragraph{Why CIFAR-10 is milder than MNIST, with a caveat.}
CIFAR-10's higher intra-class visual diversity weakens conditional style
dependence, and $\Delta_{\mathrm{inter}}$/LP confirm this cleanly
(Experiment 4). But Experiment 5 shows the practical remedy does not
inherit this cleanly: the class-conditional Gaussian prior that perfectly
fixes MNIST generation only partially fixes CIFAR-10 generation, and
CIFAR-10 samples remain visibly blurry (FID $\sim\!83$--$84$, well above
photorealistic ranges). We avoid describing CIFAR-10 naive-sampling output
as ``visually plausible''; at this FID the more accurate description is
``recognizable but not sharp,'' and under the $\tau{=}0.25$
class-conditional prior several classes are not reliably identifiable by
eye despite scoring well on the auxiliary classifier
(Figure~\ref{fig:cifar-sampling-grid}).

\paragraph{Global vs.\ conditional regularization.}
Global aggregate matching is standard in WAEs and disentanglement models;
our results show it is insufficient whenever the style variable must be
class-invariant for generation. Per-class style MMD is a supervised
analogue of total correlation penalties, targeting the single dependency
$\zs\not\perp y$ directly rather than dependence among latent dimensions in
general.

\paragraph{The reconstruction--generation gap.}
The MNIST checkpoint reaches SSIM $0.983$ yet FID $73.99$ under naive prior
sampling (Figure~\ref{fig:recon-vs-gen}). Reconstruction quality measures
fidelity on the posterior manifold, not the prior; evaluation of generative
clustering models should report both, since Section~\ref{sec:cross-model-baseline}
shows the two can diverge substantially.

\subsection{Limitations}
\label{sec:limitations}

We list what this paper does \emph{not} establish, rather than leave it
implicit; several of these are previewed in the Scope paragraph of
Section~\ref{sec:intro} and restated here in full.
(1) Baseline comparisons
(Table~\ref{tab:cifar-main}) are single-seed for the baselines; \ours{} uses
both latent-shaping and a generative prior where each of the seven
matched-supervision baselines uses only one, and we did not equalize
per-method tuning or compute, so the CIFAR-10 comparison is evidence of
competitiveness and non-degeneracy, not a controlled superiority claim.
(2) The cross-model comparison (Experiment 1) does not control for encoder
capacity or training budget between \ours{} and the four baselines.
(3) Per-class style MMD is a supervised, label-guided regularizer; we do not
report a component-wise ablation of \ours{} beyond the $\delta$ axis (e.g.,
removing the semantic variable or the class MMD term). (4) Generation
self-accuracy is confounded by dataset-intrinsic classification difficulty
and should not be used alone as a cross-dataset leakage-severity proxy
(Experiment 4). (5) The class-conditional style prior's failure to fully
transfer to CIFAR-10 (Experiment 5) is measured on one checkpoint per
dataset; we have not tested whether a richer conditional (e.g., a per-class
mixture rather than a single Gaussian) closes the gap. (6) The robustness
check (Experiment 7) perturbs one variable at a time; we have not tested
joint perturbations (e.g.\ small style dimension \emph{and} no classifier
together), which could compound rather than simply add given how much larger
each individual effect is on CIFAR-10 than MNIST.
\pj{(7) Experiment 8's remedy comparison is MNIST-only; whether gradient
reversal's term-2 advantage and the joint remedy's term-4 advantage
replicate on CIFAR-10 is untested. (8) Three datasets remain too few to
treat the diversity--leakage relationship of Experiment 4 as a measured
correlation rather than a consistent ordering.}

\pj{\textbf{Resolved since the previous draft.} The same-model evaluation
gap (self-accuracy and the swap were both scored by the model's own
classifier) is closed by the external classifiers of
Section~\ref{sec:exp1-ext}; the uncalibrated term-4 proxy by the permutation
null of Table~\ref{tab:jointmmd}; the conditioning-vs-shrinkage confound in
the post-hoc prior by the controls in Table~\ref{tab:sampling}; the
single-seed caveat on the $\delta$ sweep and the perturbation study by
3-seed replication (Tables~\ref{tab:tradeoff},~\ref{tab:capacity-audit});
and the off-joint-support objection to the latent swap by the
support-graded intervention of Table~\ref{tab:support-swap}.}

% ============================================================
\section{Conclusion}
% ============================================================

Conditional style leakage is a failure mode in factorized generative
models: global aggregate regularization of a style variable does not
prevent it from becoming class-dependent, because marginal matching is
compatible with $\zs$ being maximally informative about the class label,
not just weakly correlated with it (Proposition~\ref{prop:marginal-not-sufficient}).
We measured this gap with a diagnostic across five model families and
showed it is statistically real even when not visually obvious. A
controlled latent-swap intervention shows the decoder does not merely have access
to the leaked information but is measurably more sensitive to it than to the
semantic code (off-diagonal content-following rises from $0.1\%$ to $60.9\%$
on MNIST once the remedy is applied). Perturbing latent capacity, training
curriculum, prior geometry, and supervision one at a time on both MNIST and
CIFAR-10, no single perturbation eliminates the leakage, so it is not
attributable to any one of these choices alone -- though on CIFAR-10 its
magnitude is highly sensitive to capacity (LP falls by up to $42.9$ points,
vs.\ at most $7.4$ on MNIST), a sensitivity a MNIST-only check would have
missed. \pj{Comparing four mechanistically different term-2 remedies at
matched budget settles what the decomposition predicts and a single remedy
could not show: gradient reversal drives linear-probe recovery to $21\%$,
less than half of per-class style MMD's $42.6\%$, yet naive factorized
sampling improves only from $0.735$ to $0.79$ and within-class
semantic--style dependence does not move at all. Term 2 and generation
quality are dissociable; a remedy that targets term 4 instead reaches
$0.91$ naive self-accuracy while leaving the probe higher than gradient
reversal does. Eliminating conditional style leakage is necessary but not
sufficient, exactly as Corollary~\ref{cor:validity} says.} Per-class style
MMD cuts inter-class style separation by
$67$--$77\%$ at the originally-measured $\delta{=}1$ and, across a fuller
six-point $\delta$ sweep, reduces it smoothly on both datasets; the
clustering cost is dataset-dependent and, at a single seed per $\delta$,
noisier than the original two-point comparison suggested -- small across
most of the sweep on both datasets, with one large outlier per dataset we
cannot yet attribute to $\delta$ itself rather than seed variance. Per-class
style MMD also raises MNIST naive-sampling self-accuracy from $15\%$ to
$70\%$ ($100\%$ combined with the post-hoc style prior). Critically, the remedy
that fully repairs MNIST generation only partially repairs CIFAR-10
generation, so fixing the diagnostic finding on one dataset does not
automatically fix the underlying generative gap on another. Factorized
generative models should verify class-invariance of style variables via
conditional diagnostics, not marginal statistics alone, and should report
where proposed remedies do and do not generalize rather than presenting a
single-dataset result as general.

% ============================================================
\bibliographystyle{plainnat}
\bibliography{references}
% ============================================================

\appendix

\section{Architecture Details}
\label{app:arch}

The semantic head produces $(\tilde\muc,r_c)$, giving
$\muc=\tilde\muc/\|\tilde\muc\|_2$ and concentration
$\rho_c=\sigma(r_c)(1-\varepsilon)$; the semantic latent is sampled by the
Spherical Cauchy M\"obius reparameterization $\eta_c\sim\mathrm{Unif}(\Sph^{d_c-1})$,
$\zc=T_{\muc,\rho_c}(\eta_c)$. The style head produces $(\mus,\log\sigma_s^2)$
and $\zs=\mus+\sigma_s\odot\varepsilon_s$, $\varepsilon_s\sim\mathcal N(0,I)$.
The ResNet-18 backbone for $32\times32$ inputs replaces the first
$7\times7$ convolution (stride 2) with a $3\times3$ convolution (stride 1,
padding 1) and removes the max-pool layer, yielding a $4\times4$ spatial
output with 512 channels; a shared FC layer maps
$512\times4\times4{=}8{,}192$ to 256 dimensions with SiLU, from which
separate linear heads produce the semantic and style parameters. The
decoder concatenates $[\zc;\zs]$ ($d_c{=}64$, $d_s{=}128$ in all
experiments) and maps it through three ResBlockUp stages (bilinear
$2\times$ upsample, $1\times1$ projection, GroupNorm+SiLU+Conv residual
block; $512{\to}256{\to}128{\to}64$ channels, $4\times4\to32\times32$),
followed by a $3\times3$ output convolution and sigmoid. For each class
$k$, \ours{} maintains a center $m_k\in\Sph^{d_c-1}$ with prior
$p(\zc\mid y{=}k)=\SCauchy(m_k,\rho_p)$, $\rho_p{=}0.7$, updated by EMA from
batch class-mean directions once per epoch,
$m_k\leftarrow\mathrm{normalize}(\tau m_k+(1-\tau)\bar\muc^{(k)})$,
$\tau{=}0.95$. The full training objective is
\begin{align}
  \mathcal{L} &= \mathcal{L}_{\mathrm{rec}} + \alpha(t)\mathcal{L}_{\mathrm{class}}
   + \beta(t)\mathcal{L}_{\mathrm{agg}} + \gamma(t)\mathcal{L}_{\mathrm{style}} \nonumber\\
  &\quad + \delta(t)\mathcal{L}_{\mathrm{style\text{-}cls}} + \eta(t)\mathcal{L}_{\mathrm{cls}},
\end{align}
with $\mathcal{L}_{\mathrm{rec}}=\lambda_1\|x-\hat x\|_1+\lambda_{\mathrm{LPIPS}}\mathrm{LPIPS}(x,\hat x)$,
$\mathcal{L}_{\mathrm{class}}=\frac1K\sum_k\MMD^2(Q_k,P_k)$ with
$Q_k=\{\zc^i{:}y_i{=}k\}$, $P_k\sim p(\zc\mid y{=}k)$,
$\mathcal{L}_{\mathrm{agg}}=\MMD^2(\{\zc^i\},\{z_{c,r_j}^p\})$ against a
random-class prior mixture, and $\mathcal{L}_{\mathrm{cls}}=\CE(h(\muc),y)$
a linear auxiliary classifier preventing semantic collapse.

\section{Training Hyperparameters}
\label{app:training}

\begin{table}[h]
\centering
\small
\caption{Hyperparameters used in all \ours{} experiments.}
\label{tab:hyperparams}
\begin{tabular}{ll}
\toprule
Parameter & Value \\
\midrule
Optimizer & Adam, $\mathrm{lr}=10^{-3}$ \\
LR schedule & StepLR, step 100, $\gamma=0.5$ \\
Batch size & 128 \\
Total epochs & 300 \\
Gradient clip (global norm) & 1.0 \\
Semantic dim $d_c$ / style dim $d_s$ & 64 / 128 \\
Prior concentration $\rho_p$ & 0.7 \\
EMA momentum $\tau$ & 0.95 \\
$\lambda_1$ / $\lambda_{\mathrm{LPIPS}}$ & 1.0 / 0.1 \\
$\alpha_f$ / $\beta_f$ (class / agg.\ MMD) & 2.0 / 5.0 \\
$\gamma_f$ (style) & 1.0 \\
$\delta_f$ (per-class style, off/on) & 0 / 1 \\
$\eta_f$ (classifier) & 0.3 \\
Style prior temperature $\tau_s$ & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

Baselines (ResNetAE, VAE, WAE-MMD, VaDE, \simple{}) use a unified
lightweight CNN protocol, 50 epochs, Adam lr $10^{-3}$, StepLR(30, 0.5); the
cross-model diagnostic baselines of Experiment 1 use a separate lightweight
3-layer CNN encoder, 100 epochs (see Section~\ref{sec:cross-model} for the
resulting comparability caveat).

\section{Phase Schedule Details}
\label{app:schedule}

\begin{table}[h]
\centering
\small
\caption{Training phase boundaries and active loss terms; ramps are linear.}
\label{tab:phases}
\begin{tabular}{lll}
\toprule
Phase & Epochs & Terms and ramp \\
\midrule
A & 0--49   & $\mathcal{L}_{\mathrm{rec}}$ only \\
B & 50--99  & $+\mathcal{L}_{\mathrm{style}}(0{\to}\gamma_f)$,
              $\mathcal{L}_{\mathrm{style\text{-}cls}}(0{\to}\delta_f)$,
              $\mathcal{L}_{\mathrm{cls}}(0.1{\to}0.2)$ \\
C & 100--199 & $+\mathcal{L}_{\mathrm{class}}(0{\to}\alpha_f/2)$,
               $\mathcal{L}_{\mathrm{agg}}(0{\to}\beta_f/2)$,
               $\mathcal{L}_{\mathrm{cls}}(0.2{\to}0.3)$ \\
D & 200--299 & $\mathcal{L}_{\mathrm{class}}(\alpha_f/2{\to}\alpha_f)$,
               $\mathcal{L}_{\mathrm{agg}}(\beta_f/2{\to}\beta_f)$ \\
\bottomrule
\end{tabular}
\end{table}

Per-class style regularization ramps up in Phase B, before
$\mathcal{L}_{\mathrm{class}}$ and $\mathcal{L}_{\mathrm{agg}}$ are
introduced in Phase C, so that per-class style regularization begins as
early as possible relative to the semantic MMD terms introduced later,
limiting -- rather than eliminating -- the epochs during which
class-specific $(\zc,\zs)$ interactions can form without any
style-conditional counter-pressure. (Phase A itself, epochs 0--49, is
reconstruction-only, so the decoder does see real $(\zc,\zs)$ pairs before
any style or class regularization is active.)

\section{t-SNE of the Style Latent Across Model Families}
\label{app:tsne}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\linewidth]{tsne_zs_six_models.png}
\caption{t-SNE of the style latent $\zs$, colored by MNIST digit class, for
         all six rows of Table~\ref{tab:cross-model}. Unlike a typical
         clustering figure, \emph{color-separated clusters here mean worse}
         (class information leaking into $\zs$): separation is visually
         obvious only for WAE-MMD and \ours{} (no per-class MMD)---the two
         rows with the \emph{largest} $\Delta_{\mathrm{inter}}$ in
         Table~\ref{tab:cross-model}---and visibly reduced (improved) after
         adding per-class style MMD. For VAE, $\beta$-TCVAE, and FactorVAE
         the leakage is statistically real (LP $74$--$87\%$) but not
         visually apparent in this 2D projection. Referenced from
         Experiment 1 (Section~\ref{sec:cross-model}).}
\label{fig:tsne}
\end{figure*}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{conditional_mmd_projection_histogram_mnist.png}
\caption{An alternative to the t-SNE view above, for \ours{} at
         $\delta{=}0$: project $\zs$ onto the top principal component of
         the $K\times d_s$ class-mean matrix (the same signal
         $\Delta_{\mathrm{inter}}$ summarizes into one scalar) and plot
         per-class histograms. Some classes separate cleanly along this
         single direction even where a 2D t-SNE embedding does not make the
         separation visually obvious, illustrating that a fixed
         low-dimensional projection can either overstate or understate
         leakage depending on which directions it happens to preserve --
         another reason to prefer the quantitative diagnostics of
         Section~\ref{sec:diagnostics} over any single plot.}
\label{fig:cmmd-projection}
\end{figure}

\begin{figure}[t]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{conditional_mmd_pairwise_heatmap_mnist.png}
  \caption{MNIST}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{conditional_mmd_pairwise_heatmap_cifar10.png}
  \caption{CIFAR-10}
\end{subfigure}
\caption{Pairwise $\MMD^2(q(\zs\mid y{=}i),q(\zs\mid y{=}j))$ between all
         class pairs, \ours{} at $\delta{=}0$. Off-diagonal structure shows
         which class pairs occupy the most visibly different regions of
         $\zs$; both datasets show broad off-diagonal separation, not a few
         isolated confusable pairs, consistent with the per-class bar chart
         of Figure~\ref{fig:cmmd-bar} showing every class contributes to
         the leakage rather than a small subset.}
\label{fig:cmmd-pairwise}
\end{figure}

\begin{figure}[t]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{cifar10_swap_content_heatmap_delta0.png}
  \caption{Content-following, $\delta{=}0$}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{cifar10_swap_style_heatmap_delta0.png}
  \caption{Style-following, $\delta{=}0$}
\end{subfigure}

\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{cifar10_swap_content_heatmap_delta1.png}
  \caption{Content-following, $\delta{=}1$}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \includegraphics[width=\linewidth]{cifar10_swap_style_heatmap_delta1.png}
  \caption{Style-following, $\delta{=}1$}
\end{subfigure}
\caption{CIFAR-10 counterpart to Figure~\ref{fig:swap-heatmaps}. The
         $\delta{=}0\to\delta{=}1$ shift is the same direction as MNIST but
         weaker in magnitude (content-following $4.2\%\to20.0\%$,
         style-following $68.8\%\to38.5\%$), consistent with
         Section~\ref{sec:swap}'s reading that CIFAR-10's decoder was never
         as exclusively dependent on $\zs$ for identity to begin with.}
\label{fig:cifar-swap-heatmaps}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{cifar10_swap_grid_delta0.png}
\caption{CIFAR-10 latent-swap grid, $\delta{=}0$ (cf.\
         Figure~\ref{fig:swap-grid} for MNIST). Rows are visually more
         similar to each other than in the MNIST grid but still show
         column-driven (style-driven) drift in object identity for several
         classes.}
\label{fig:cifar-swap-grid}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{mnist_sampling_strategy_bars.png}
\caption{MNIST sampling strategies (Table~\ref{tab:sampling}) plotted
         directly: self-accuracy jumps from near-chance to $100\%$ the
         moment $\zs$ is conditioned on class in any way, while diversity
         (right axis) is lowest for the class-mean point estimate and
         highest for the empirical style bank.}
\label{fig:mnist-sampling-bars}
\end{figure}

\begin{figure}[t]
\centering
\begin{subfigure}[t]{0.24\linewidth}
  \includegraphics[width=\linewidth]{mnist_sampling_global_gaussian.png}
  \caption{Global\\(15\%)}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.24\linewidth}
  \includegraphics[width=\linewidth]{mnist_sampling_class_mean.png}
  \caption{Mean\\(100\%)}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.24\linewidth}
  \includegraphics[width=\linewidth]{mnist_sampling_class_diag_t025.png}
  \caption{$\tau{=}0.25$\\(100\%)}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.24\linewidth}
  \includegraphics[width=\linewidth]{mnist_sampling_class_empirical.png}
  \caption{Empirical\\(100\%)}
\end{subfigure}
\caption{MNIST generation under representation-aware style sampling. Global
         Gaussian produces shapeless or wrong-class images; all
         class-conditional strategies restore correct digit identity.}
\label{fig:sampling-strategies}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{cifar10_sampling_strategy_bars.png}
\caption{CIFAR-10 analogue of Figure~\ref{fig:mnist-sampling-bars}
         (Table~\ref{tab:cifar-sampling}). The class-conditional Gaussian
         strategies that reach $100\%$ on MNIST plateau near $70\%$ here;
         only resampling real encoded style codes (empirical bank)
         approaches MNIST-level self-accuracy.}
\label{fig:cifar-sampling-bars}
\end{figure}

\end{document}
