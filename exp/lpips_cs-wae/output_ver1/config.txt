{
    "latent_dim": 32,
    "n_classes": 10,
    "batch_size": 128,
    "epochs": 50,
    "lr": 1e-3,
    "rho_prior": 0.5,
    "epsilon": 1e-8,
    # --- Cấu hình cho Loss ---
    "bce_weight": 0.5,      # <<< THAY ĐỔI: Trọng số cho BCE loss
    "lpips_weight": 0.5,    # <<< THAY ĐỔI: Trọng số cho LPIPS loss
    # --- Cấu hình cho Annealing ---
    "sup_mmd_weight": 20.0,
    "unsup_mmd_weight": 50.0,
    "anneal_epochs": 25,
}