\section{Proofs}
\label{app:proofs}

\subsection{Proof of Proposition~\ref{prop:nocert}}

The label is defined after drawing $\zs$ and therefore does not alter the
marginal law of $\zs$.  Hence $q(\zs)=\mathcal N(0,I)$ and any divergence
between these two identical measures is zero.  Because $y$ is a deterministic
function of $\zs$, $H(y\mid\zs)=0$.  The equal-probability partition gives
$H(y)=\log K$, and consequently
$I(\zs;y)=H(y)-H(y\mid\zs)=\log K$.

\subsection{A smooth, full-support construction}

Let $z\sim\mathcal N(0,1)$ and, conditional on $z$, draw a binary label with
\begin{equation}
P(y=1\mid z)=\sigma(az),\qquad P(y=0\mid z)=\sigma(-az),
\end{equation}
where $a>0$ and $\sigma(t)=(1+e^{-t})^{-1}$.  Since the standard normal
density $\phi$ is symmetric and $\sigma(t)+\sigma(-t)=1$,
\begin{align}
P(y=1)
&=\int_{\mathbb R}\phi(z)\sigma(az)\,dz\\
&=\frac12\int_{\mathbb R}\phi(z)
\bigl[\sigma(az)+\sigma(-az)\bigr]\,dz=\frac12.
\end{align}
The generative construction starts with the standard normal and changes only
the conditional label law, so the marginal of $z$ remains exactly standard
normal.  Bayes' rule gives
\begin{equation}
q(z\mid y=1)=2\phi(z)\sigma(az),\qquad
q(z\mid y=0)=2\phi(z)\sigma(-az).
\end{equation}
Both densities are smooth and strictly positive on $\mathbb R$.  When
$a\rightarrow\infty$, the conditional label approaches the indicator of the
sign of $z$ outside a set of Gaussian measure zero.  Thus
$H(y\mid z)\rightarrow0$ and $I(z;y)\rightarrow\log2$ while the marginal
remains unchanged.

\subsection{Proof of Theorem~\ref{thm:decomp}}

For a fixed $y$, apply the relative-entropy chain rule to the encoded joint
and the product reference measure:
\begin{align}
&\KL\!\left(q(\zc,\zs\mid y)\|p(\zc\mid y)p(\zs)\right)\\
={}&\KL\!\left(q(\zc,\zs\mid y)\|q(\zc\mid y)q(\zs\mid y)\right)\nonumber\\
&+\KL(q(\zc\mid y)\|p(\zc\mid y))
+\KL(q(\zs\mid y)\|p(\zs)).
\end{align}
The first term is $I_q(\zc;\zs\mid y)$ at that label.  Averaging over
$q(y)$ gives terms 3 and 4 of Eq.~\ref{eq:decomp}.  For the remaining style term,
insert the aggregate posterior $q(\zs)$:
\begin{align}
\mathbb E_{q(y)}\KL(q(\zs\mid y)\|p(\zs))
={}&\mathbb E_{q(y)}\KL(q(\zs\mid y)\|q(\zs))\nonumber\\
&+\KL(q(\zs)\|p(\zs))\\
={}&I_q(\zs;y)+\KL(q(\zs)\|p(\zs)).
\end{align}
These are identities of non-negative extended-real quantities and require no
subtraction of infinities.  Applying the same chain rule to the joint law
$q(y,\zs)$ and reference $q(y)p(\zs)$ yields the style identity above, so the
full decomposition holds in $[0,\infty]$.  If $M_{\mathrm{fact}}<\infty$, the
non-negative summands on the right are bounded by their finite sum; hence all
four are finite and the identity is ordinary real-valued equality.
Non-negativity follows from non-negativity of relative entropy and mutual
information.

\subsection{Proof of Corollary~\ref{cor:valid}}

An expectation of a non-negative KL is zero iff its integrand is zero for
$q(y)$-almost every $y$, which holds iff the two latent conditional measures
are equal.  The pushforward inequality follows by applying the data-processing
inequality to the decoder Markov kernel for each $y$, then averaging over
$q(y)$.  This covers deterministic and stochastic decoders.

\section{Architecture and Training}
\label{app:model}

\subsection{Architecture}

The shared encoder is a ResNet-18 adapted to small images: its first
$7\times7$, stride-2 convolution is replaced by a $3\times3$, stride-1
convolution and max pooling is removed.  The trunk output is flattened and
mapped to 256 dimensions with SiLU.  Separate heads produce an unnormalized
semantic direction, a scalar semantic concentration, a style mean, and a
style log variance.  We use
\begin{align}
\muc&=\widetilde\muc/\|\widetilde\muc\|_2, &
\rho_c&=\sigma(r_c)(1-\varepsilon),\\
\zs&=\mus+\exp(\tfrac12\log\sigma_s^2)\odot\epsilon,
&\epsilon&\sim\mathcal N(0,I).
\end{align}
Here $\varepsilon=10^{-8}$ only enforces $\rho_c<1$.  With
$e\sim\operatorname{Unif}(\mathbb S^{d-1})$, the implemented Spherical Cauchy
sample is the normalized M\"obius transform
\begin{equation}
z=\operatorname{norm}\!\left(
\frac{(1-\rho^2)e+2\rho^2\mu+2\rho(e^\top\mu)\mu}
{1+2\rho e^\top\mu+\rho^2+\varepsilon}\right),
\end{equation}
following the efficient Spherical Cauchy construction of
\citet{sablica2025hyperspherical}.  The decoder concatenates $[\zc;\zs]$, projects it to a
$512\times4\times4$ tensor, and applies three residual bilinear-upsample
blocks with channel widths $256,128,64$.  Its sigmoid output is interpolated
to the native dataset resolution when necessary.  MNIST and Fashion-MNIST
therefore remain $28\times28$ and single-channel; CIFAR-10 remains
$32\times32\times3$.

For class $k$, the semantic prior is a Spherical Cauchy distribution centered
at an EMA direction $m_k$, with concentration $\rho_p=0.7$.  The center is
updated once per epoch from encoded class-mean directions with momentum
$0.95$.  The style prior is $\mathcal N(0,I)$.

\subsection{Objective}

The training objective is
\begin{align}
\mathcal L={}&\mathcal L_{\mathrm{rec}}
+\alpha(t)\mathcal L_{\mathrm{class}}
+\beta(t)\mathcal L_{\mathrm{agg}}
+\gamma(t)\mathcal L_{\mathrm{style}}\nonumber\\
&+\delta(t)\mathcal L_{\mathrm{style-cls}}
+\eta(t)\mathcal L_{\mathrm{cls}}.
\end{align}
$\mathcal L_{\mathrm{rec}}$ is L1 plus $0.1$ LPIPS
\citep{zhang2018unreasonable}.  $\mathcal L_{\mathrm{class}}$ matches each
semantic class conditional to its spherical prior, $\mathcal L_{\mathrm{agg}}$
matches aggregate semantic codes to the class-prior mixture,
$\mathcal L_{\mathrm{style}}$ matches sampled style codes to a standard
Gaussian, and $\mathcal L_{\mathrm{cls}}$ is cross entropy from $\muc$.
The baseline checkpoints audited in the main paper explicitly set
$\delta=0$; the repository default for new remedied runs is $\delta=1$, so
the run manifest or command-line override is required to distinguish them.

\begin{table}[ht]
\caption{Case-study training hyperparameters.}
\label{tab:hyperparameters}
\centering
\small
\begin{tabular}{ll}
\toprule
Parameter & Value \\
\midrule
Optimizer & Adam, learning rate $10^{-3}$ \\
Schedule & StepLR, step 100, factor $0.5$ \\
Batch size / epochs & 128 / 300 \\
Gradient clipping & global norm 1.0 \\
Semantic / style dimension & 64 / 128 \\
Semantic-prior concentration & 0.7 \\
EMA momentum & 0.95 \\
Final class / aggregate MMD weights & 2.0 / 5.0 \\
Final global style MMD weight & 1.0 \\
Final auxiliary-classifier weight & 0.3 \\
\bottomrule
\end{tabular}
\end{table}

Training has four phases: reconstruction only for epochs 0--49; global style
MMD and the auxiliary classifier ramp during epochs 50--99; semantic class
and aggregate MMD ramp to half strength during epochs 100--199; all terms
reach full strength during epochs 200--299.  For remedied runs, per-class
style MMD follows the global-style ramp.  No image augmentation is used.

\section{Canonical Audit Protocol}
\label{app:protocol}

\subsection{Data split and latent views}

For each checkpoint we select 2,048 examples from the standard test split
with evaluation seed 0.  Probe indices are generated by a deterministic
stratified 60/20/20 split, yielding 1,228 training, 410 validation, and 410
test examples.  Standardization parameters are estimated only on the 1,228
probe-training examples.  The split indices are stored by SHA-256 hash in
each result manifest.

The encoder is evaluated once to obtain $(\muc,\mus,\log\sigma_s^2)$ and a
seeded stochastic draw $\zs$.  We use:
\begin{itemize}
    \item $\mus$ for representation probes and deterministic mean swaps;
    \item $\zs$ for global MMD, conditional style statistics, and the
    sampling-contract probe; and
    \item both $(\muc,\mus)$ and repeated posterior draws $(\zc,\zs)$ for
    the conditional-dependence diagnostic.
\end{itemize}
Agreement between these views is reported as an empirical result rather than
assumed.

\subsection{Probe suite}

The suite contains multinomial logistic regression, a fixed two-layer MLP,
RBF-SVM, and $k$-NN.  Validation data are used for fixed early-stopping or
model-selection rules; every headline accuracy is evaluated once on the
untouched test partition.  We report accuracy and macro-F1.  The main paper
uses logistic regression as the simplest standardized headline and includes
RBF-SVM to detect nonlinear separation.

\subsection{MMD and HSIC}

Global MMD is the unbiased U-statistic with the averaged Euclidean RBF kernel
\begin{equation}
k(u,v)=\frac17\sum_{\sigma\in\{0.5,1,2,5,10,20,50\}}
\exp\!\left(-\frac{\|u-v\|_2^2}{2\sigma^2}\right).
\end{equation}
The Gaussian reference has the same sample count as the encoded set and uses
an RNG stream independent of posterior sampling.  A balanced pooled-label
permutation test calibrates the finite-sample null with 500 permutations.
The resulting null 95th percentiles are $1.88$--$2.45\times10^{-5}$, whereas
observed global MMD is $2.48$--$5.79\times10^{-4}$; exact equality is rejected
for every checkpoint ($p=1/501$).  We therefore use ``small'' only as a
relative numerical description, not as acceptance of the equality null.
$\Dinter$ is not an MMD; it is the average Euclidean distance between all
pairs of class-conditional style means.

Label HSIC centers a multiscale style Gram matrix and a label Gram matrix and uses
1,024 examples for bounded memory.  We calibrate it by 200 label
permutations.  With the plus-one correction,
\begin{equation}
p=\frac{1+\#\{T_b\ge T_{\mathrm{obs}}\}}{201},
\end{equation}
so $p=0.004975$ is reported rather than $p=0$.  For the within-class
permutation statistic, with represented classes $\mathcal K$ and empirical
weights $\omega_k$, we use
\begin{equation}
T_{\mathrm{WC}}=\sum_{k\in\mathcal K}\omega_k
\HSIC(\zc^{(k)},\zs^{(k)}).
\label{eq:within-class-hsic}
\end{equation}
Content vectors are
normalized to the unit sphere and styles are standardized within class.
We use the chordal multiscale kernel with bandwidths
$\{.1,.3,.5,1,2\}$ for content and the Euclidean ladder above for style.
Each classwise Gram matrix is centered and classwise HSIC is divided by
$(n_k-1)^2$.  The reported statistic weights classes by
$\omega_k=q(y=k)$, estimated as $n_k/\sum_j n_j$, and every null replicate
independently permutes the style Gram matrix within each class before
recomputing the same weighted sum.  Because every audit population used here
is balanced, this equals the unweighted macro average numerically.

The historical JointMMD proxy paired each observation with a permutation of
the same style bank and is therefore not used as a headline independent
two-sample test.  The split-bank diagnostic in
Appendix~\ref{app:complete-contract} resolves this issue: posterior and
prior banks are independently sampled, the content-prior MMD uses the same
chordal ladder, and the direct-joint MMD uses the product of content and style
kernels.  Both aggregate their balanced class statistics before permutation
calibration; per-class decisions use Holm correction.

\section{Full Stage-0 Results}
\label{app:full-results}

\begin{table}[ht]
\caption{Full held-out probe suite from the original audit.  Values are test
accuracy in percent.  MNIST/Fashion-MNIST rows are seed-0 details; CIFAR-10 is
mean $\pm$ sample standard deviation over model seeds 0, 1, 2.}
\label{tab:full-probes}
\centering
\small
\begin{tabular}{llrrrr}
\toprule
Dataset & View & Logistic & MLP & RBF-SVM & $k$-NN \\
\midrule
MNIST & $\mus$ & 99.76 & 99.51 & 99.76 & 97.80 \\
      & $\zs$  & 99.76 & 99.51 & 99.76 & 97.80 \\
Fashion & $\mus$ & 88.29 & 90.73 & 93.17 & 85.12 \\
        & $\zs$  & 88.29 & 90.73 & 93.17 & 85.61 \\
CIFAR-10 & $\mus$ & $68.46\pm6.29$ & $70.49\pm3.83$ &
$70.24\pm3.23$ & $42.52\pm6.93$ \\
         & $\zs$ & $68.62\pm6.19$ & $70.57\pm3.50$ &
$70.16\pm3.36$ & $42.20\pm7.11$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\caption{Completed three-seed headline replication for the original MNIST and
Fashion-MNIST checkpoints.  Values are rounded aggregate results; every
seedwise HSIC test attains $p=1/201$.}
\label{tab:mnist-fashion-aggregate}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Dataset & Global MMD & Logit $z_s$ (\%) & Cond. MMD & HSIC $p$ \\
\midrule
MNIST & .00058 & 99.6 & .0160 & .00498 \\
Fashion-MNIST & .00034 & 88.0 & .010 & .00498 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\caption{Per-seed CIFAR-10 results for the two principal probes.}
\label{tab:cifar-seeds}
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
Seed & $\Dinter(\zs)$ & Logit $\mus$ & Logit $\zs$ & RBF $\mus$ & RBF $\zs$ \\
\midrule
0 & 3.801 & 61.46 & 61.71 & 66.59 & 66.34 \\
1 & 4.264 & 73.66 & 73.66 & 72.68 & 72.68 \\
2 & 3.797 & 70.24 & 70.49 & 71.46 & 71.46 \\
\bottomrule
\end{tabular}
\end{table}

For term 4, ten stochastic posterior draws per checkpoint all attain the
minimum calibrated $p=1/201$.  The sample-view statistic is
$0.002682\pm0.000005$ across draws on MNIST, $0.002459$ on Fashion-MNIST,
and has per-model means $0.002250$, $0.002292$, and $0.002338$ on CIFAR-10.
The seed-0 $\delta=1$ checkpoints also reject on all ten draws, with means
$0.002507$ (MNIST) and $0.002283$ (CIFAR-10).  These calibrated kernel tests
are evidence of stochastic within-class dependence, not numerical estimates
of $I(\zc;\zs\mid y)$.

\subsection{Structure of the conditional mismatch}

The class averages reported for the original checkpoints compress a
structured ten-class discrepancy.  Figure~\ref{fig:pairwise-mmd-app} compares every
pair of class-conditional style distributions.  Broad off-diagonal mass is
visible on both datasets, rather than separation being confined to a few
class pairs.  These matrices use the same stochastic-view diagnostic as the
per-class prior comparison; their scale should not be compared across
datasets as if it were a normalized dependence measure.

\begin{figure}[t]
\centering
\begin{minipage}{0.47\textwidth}
\centering
\includegraphics[width=\linewidth,trim=0 0 0 70,clip]{conditional_mmd_pairwise_heatmap_mnist.png}\\[-0.4em]
\small (a) MNIST
\end{minipage}\hfill
\begin{minipage}{0.47\textwidth}
\centering
\includegraphics[width=\linewidth,trim=0 0 0 70,clip]{conditional_mmd_pairwise_heatmap_cifar10.png}\\[-0.4em]
\small (b) CIFAR-10
\end{minipage}
\caption{Pairwise $\MMD^2(q(\zs\mid y=i),q(\zs\mid y=j))$ for the
baseline seed-0 checkpoints.  The diagonal is zero by construction;
off-diagonal structure shows that conditional style distributions differ
throughout the label set.}
\label{fig:pairwise-mmd-app}
\end{figure}

Figure~\ref{fig:projection-app} gives a complementary one-dimensional view.
The projection direction is computed from the class means rather than chosen
after inspecting individual examples.  Several MNIST classes separate along
this direction, but the overlap also illustrates why a single projection is
not a substitute for the held-out multivariate probes.

\begin{figure}[t]
\centering
\includegraphics[width=0.78\textwidth,trim=0 0 0 80,clip]{conditional_mmd_projection_histogram_mnist.png}
\caption{MNIST stochastic styles projected onto the leading direction of
the class-mean matrix at $\delta=0$.  The direction explains 13.1\% of the
class-mean variance.  The plot is descriptive; the held-out probes and HSIC
tests provide the quantitative evidence.}
\label{fig:projection-app}
\end{figure}
\FloatBarrier

\subsection{Posterior-variance inspection}

The fractions of raw style log-variance coordinates at or below the clamp
floor are 99.9966\% (MNIST), 99.9989\% (Fashion-MNIST), and 100\% for all
three CIFAR-10 checkpoints.  Because this statistic was added during the
post-hoc checkpoint audit, it is reported in the human-readable audit report;
future manifests should also store it as a first-class numeric field.

\subsection{Complete conditional-regularization sweep}

Table~\ref{tab:full-delta-sweep} expands the four points shown in the main
paper to all six archived $\delta$ settings.  We retain ACC, $\Dinter$, and
FID because they are directly tied to the sweep artifacts, but omit the
historical probe column until it is regenerated with the canonical held-out
split.  Each point is a separate single-seed training run.

\begin{table}[ht]
\caption{Complete single-seed per-class-style-MMD sweep.  Lower FID and
$\Dinter$ are better; no uncertainty or significance claim is made.}
\label{tab:full-delta-sweep}
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{3}{c}{MNIST} & \multicolumn{3}{c}{CIFAR-10} \\
$\delta$ & ACC & $\Dinter$ & FID & ACC & $\Dinter$ & FID \\
\midrule
0    & .9915 & 6.21 & 74.0 & .8129 & 3.74 & 80.5 \\
.03  & .9944 & 3.27 & 62.8 & .8111 & 2.48 & 79.1 \\
.1   & .9898 & 2.12 & 52.6 & .8100 & 2.03 & 77.5 \\
.3   & .9936 & 1.55 & 59.4 & .7148 & 1.59 & 72.0 \\
1    & .8592 & 1.22 & 43.4 & .8000 & 1.05 & 74.4 \\
3    & .9929 & 1.18 & 41.5 & .7994 & .95  & 76.1 \\
\bottomrule
\end{tabular}
\end{table}

The reduction in $\Dinter$ is broadly monotone, but the operating-point
quality is not: MNIST ACC falls to $.8592$ specifically at $\delta=1$ while
the neighboring $.1$ and $.3$ runs remain at $.9898$ and $.9936$, and the
$\delta=3$ run returns to $.9929$.  The completed three-seed ACC ranges are
$.985$--$.994$ at $\delta=0$, $.983$--$.993$ at $.1$, $.985$--$.995$ at $.3$,
$.850$--$.8656$ at $1$, and $.985$--$.995$ at $3$.  The degradation at
$\delta=1$ is therefore a replicated operating-regime effect rather than one
bad seed.  Because the decoder swap intervention itself is available only for
seed 0 at $\delta=1$, its large reduction in style-donor following remains a
directional mechanism check rather than evidence for a reliable remediation
setting.

\FloatBarrier

\section{Generation and Decoder Diagnostics}
\label{app:decoder}

\subsection{External generation evaluation}

The independent MNIST evaluator is a four-convolution CNN trained for 20
epochs on real images only.  Its standard test accuracy is 99.34\%.  The
CIFAR-10 evaluator is a WideResNet-28-10 trained for 200 epochs with standard
random-crop and horizontal-flip augmentation on real training images only;
its test accuracy is 96.16\%.  Neither evaluator contributes to the
generative model's loss.

\begin{table}[ht]
\caption{Per-checkpoint external Gen-ACC.  Each entry uses 1,000 images per
requested class and a class-conditional style bank fitted on 12,000 training
examples.}
\label{tab:generation-seeds}
\centering
\small
\begin{tabular}{llrr}
\toprule
Dataset & Model seed & Global Gaussian & Class diagonal $\tau=.25$ \\
\midrule
MNIST & 0 & 0.1047 & 1.0000 \\
CIFAR-10 & 0 & 0.1262 & 0.4016 \\
CIFAR-10 & 1 & 0.1320 & 0.5386 \\
CIFAR-10 & 2 & 0.1268 & 0.4747 \\
\bottomrule
\end{tabular}
\end{table}

The class-diagonal distribution is fit from encoded training styles and
therefore deliberately conditions style on the requested label.  Its gain
tests whether the decoder is sensitive to the conditional mismatch.  It is
not an unconditional generative model and does not alter the encoder.

Figure~\ref{fig:cifar-sampling-app} displays the qualitative counterpart of
Table~\ref{tab:generation-seeds}.  Conditioning style makes the requested
category more visually consistent in some rows, but shrinking the diagonal
Gaussian to $\tau=.25$ also reduces variation and can produce blurry or
prototype-like outputs.  This is why Gen-ACC is not interpreted as a
stand-alone measure of generative quality; diversity-sensitive evaluation on
the same generated pools remains necessary.

\begin{figure}[t]
\centering
\includegraphics[width=0.43\textwidth]{cifar10_sampling_grid_trimmed.png}
\caption{Archived CIFAR-10 samples from the seed-0 checkpoint.  For four
requested classes, odd rows use the global Gaussian style prior and even rows
use the post-hoc class-diagonal prior at $\tau=.25$.  The grid is qualitative;
external Gen-ACC is reported in Table~\ref{tab:generation-seeds}.}
\label{fig:cifar-sampling-app}
\end{figure}
\FloatBarrier

\subsection{Independent-evaluator deterministic mean swaps}

For each ordered pair of distinct classes $(a,b)$, the diagnostic decodes
$\mathrm{Dec}(\muc^{(a)},\mus^{(b)})$ for 500 donor pairs and evaluates pixels
with the independent real-image classifier.  On baseline checkpoints,
identity follows the style donor on 99.28\% of MNIST and 63.92\% of CIFAR-10
off-diagonal swaps, while following the semantic donor on 0.06\% and 4.72\%.
At $\delta=1$, style-following falls to 23.37\% and 35.92\%, and semantic-
following rises to 51.00\% and 18.74\%, respectively.  Full rate matrices,
the other-class outcome, and pair-bootstrap intervals are stored in the
external-swap artifacts.

\begin{figure}[t]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=0.86\linewidth]{mnist_swap_grid_delta0.png}\\[-0.4em]
\small (a) MNIST
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=0.86\linewidth]{cifar10_swap_grid_delta0.png}\\[-0.4em]
\small (b) CIFAR-10
\end{minipage}
\caption{Deterministic mean-swap grids at $\delta=0$.  Rows donate
semantic means and columns donate style means.  Column-controlled identity
is visually strong on MNIST and weaker but still present for several
CIFAR-10 classes.  Quantitative rates in the text use independent pixel
classifiers; the grids are qualitative.}
\label{fig:swap-grid-app}
\end{figure}
\FloatBarrier

\section{Exact-Marginal Experiment}
\label{app:synthetic}

\subsection{Construction and protocol}

For every replication we draw $n=2{,}048$ vectors
$z\sim\mathcal N(0,I_{128})$ once, then reuse that latent sample while drawing
binary labels from logits $[-\kappa z_1,\kappa z_1]$ for
$\kappa\in\{0,.5,1,2,4,8,16\}$.  Thus the population marginal and the
finite latent sample are invariant to $\kappa$; only the label mechanism
changes.  We run 50 replications from base seed 2027, 499 global-MMD
permutations, 499 HSIC permutations, and a stratified 60/20/20 linear-probe
split.  The independent Gaussian reference and the labels use distinct random
streams.  Plus-one calibration gives minimum $p=1/500$.

\begin{figure}[ht]
\centering
\includegraphics[width=0.96\textwidth]{exact_marginal_vs_leakage.png}
\caption{Exact marginal matching does not certify label independence.  The
same Gaussian sample is relabeled as $\kappa$ changes, so global MMD remains
invariant while probe accuracy and conditional discrepancy increase.  Points
and bars are mean $\pm$ one sample standard deviation over 50 replications.}
\label{fig:exact-marginal-app}
\end{figure}

\begin{table}[ht]
\caption{Complete synthetic result.  Probe values are mean $\pm$ sample
standard deviation over 50 replications; rejection columns are empirical
rates.  The global statistic is repeated across $\kappa$ within each
replication by design.}
\label{tab:synthetic-full}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{rrrrrrr}
\toprule
$\kappa$ & Probe (\%) & Global MMD & MMD rej. & HSIC & HSIC rej. & Cond. MMD \\
\midrule
0   & $49.58\pm1.86$ & $-9.47\!\times\!10^{-7}$ & .06 & .000176 & .02 & .000006 \\
.5  & $62.48\pm2.58$ & $-9.47\!\times\!10^{-7}$ & .06 & .000239 & 1.00 & .000130 \\
1   & $74.49\pm2.09$ & $-9.47\!\times\!10^{-7}$ & .06 & .000314 & 1.00 & .000280 \\
2   & $83.28\pm2.07$ & $-9.47\!\times\!10^{-7}$ & .06 & .000377 & 1.00 & .000405 \\
4   & $89.51\pm1.45$ & $-9.47\!\times\!10^{-7}$ & .06 & .000404 & 1.00 & .000455 \\
8   & $91.82\pm1.63$ & $-9.47\!\times\!10^{-7}$ & .06 & .000413 & 1.00 & .000479 \\
16  & $92.30\pm1.51$ & $-9.47\!\times\!10^{-7}$ & .06 & .000415 & 1.00 & .000478 \\
\bottomrule
\end{tabular}
\end{table}

The unbiased global MMD can be negative under the null.  Three of 50 global
tests reject, inside the frozen central 95\% prediction interval $[0,6]$ for
$\operatorname{Binomial}(50,.05)$.  HSIC has Spearman trend 1.0 and
conditional MMD trend .964.  Seven of eight frozen checks pass.  The failed
check required mean probe accuracy strictly above 90\% at each of
$\{4,8,16\}$; $\kappa=4$ reaches 89.51\%.  No threshold, level, or inequality
was changed after observing the result.

\begin{table}[ht]
\caption{Supplementary sample-size sensitivity at $\kappa=16$.  Values are
rounded aggregate results from the completed follow-up runs.}
\label{tab:synthetic-n}
\centering
\small
\begin{tabular}{rrr}
\toprule
$n$ & Probe accuracy (\%) & Global-MMD rejection (\%) \\
\midrule
512  & 92.0 & 6.0 \\
1,024 & 92.2 & 5.0 \\
2,048 & 92.3 & 6.0 \\
4,096 & 92.5 & 5.0 \\
\bottomrule
\end{tabular}
\end{table}

The probe effect is essentially stable across an eightfold range in sample
size, whereas the rejection rate remains near the nominal $5\%$ level.  The
follow-up therefore supports a variance-reduction interpretation rather than
an effect-size increase with $n$.

\section{Controlled Posterior-Entropy Intervention}
\label{app:entropy}

The intervention adds a fixed floor in variance space and uses the resulting
$\sigma_{\rm eff}$ in both training and audit.  A floor of zero reproduces the
historical sampler.  Every reported setting is trained for 300 epochs with
MNIST and CIFAR-10 model seeds $\{0,1,2\}$; the original seed-0 sweep below is
retained at full precision and the completed summaries follow it.  The CIFAR-10 confirmation
levels $\{0,.15,.35\}$ were fixed after both $.15$ and $.35$ passed the
pre-specified MNIST promotion rule.  That rule required an entropy increase of
at least .75 nats/dimension, probe accuracy at least .20, HSIC rejection,
conditional MMD above global MMD, and final reconstruction no more than twice
baseline.

\begin{table}[ht]
\caption{Complete MNIST entropy sweep.  Median standard deviation is the
effective posterior value; floor hit refers to the raw log-variance head.}
\label{tab:entropy-mnist-full}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{rrrrrrrrr}
\toprule
$\sigma_f$ & $H$/dim & Med. std & Raw floor & Probe & Global & Cond. & Gen-ACC & Rec. \\
\midrule
0 & -3.581 & .00674 & 100.00\% & 99.76\% & .000579 & .016024 & 10.15\% & .01259 \\
.025 & -2.215 & .02589 & 99.16\% & 99.51\% & .000599 & .016352 & 10.05\% & .01273 \\
.05 & -1.553 & .05045 & 99.17\% & 99.51\% & .000648 & .016135 & 11.95\% & .01315 \\
.15 & -.477 & .15015 & 99.77\% & 98.78\% & .000654 & .012044 & 37.35\% & .01520 \\
.35 & .381 & .35006 & 96.50\% & 95.61\% & .000518 & .007516 & 93.50\% & .01658 \\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[ht]
\caption{Completed three-seed MNIST entropy confirmation.  Probe is mean
$\pm$ sample standard deviation; the remaining entries are observed seed
ranges supplied by the completed external runs.}
\label{tab:entropy-mnist-aggregate}
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{rrrrr}
\toprule
$\sigma_f$ & Probe (\%) & Global MMD & Cond. MMD & Gen-ACC (\%) \\
\midrule
0 & $99.5\!\pm\!.4$ & $(5$--$7)\!\times\!10^{-4}$ & $.014$--$.018$ & $9$--$13$ \\
.15 & $97.5\!\pm\!1.5$ & $(6$--$8)\!\times\!10^{-4}$ & $.010$--$.014$ & $32$--$42$ \\
.35 & $93.0\!\pm\!2.5$ & $(4$--$6)\!\times\!10^{-4}$ & $.006$--$.009$ & $85$--$96$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\caption{Seed-0 detail for the fixed CIFAR-10 entropy confirmation.}
\label{tab:entropy-cifar-full}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{rrrrrrrr}
\toprule
$\sigma_f$ & $H$/dim & Med. std & Probe & Global & Cond. & Gen-ACC & Rec. \\
\midrule
0 & -3.581 & .00674 & 61.46\% & .000249 & .005789 & 12.60\% & .05140 \\
.15 & -.477 & .15015 & 28.78\% & .000743 & .003690 & 27.35\% & .05940 \\
.35 & .369 & .35006 & 9.76\% & .000667 & .001287 & 54.25\% & .06476 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\caption{Three-seed CIFAR-10 entropy confirmation.  Entries are rounded means
over model seeds; $p=.00498$ means that every seed reaches the minimum
plus-one calibrated HSIC value with 200 permutations.}
\label{tab:entropy-cifar-aggregate}
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{rrrrrrr}
\toprule
$\sigma_f$ & Probe (\%) & Global MMD & Cond. MMD & HSIC $p$ & Gen-ACC (\%) & Rec. L1 \\
\midrule
0 & 68.0 & .00030 & .0058 & .00498 & 13.0 & .052 \\
.15 & 29.0 & .00070 & .0037 & .00498 & 27.0 & .059 \\
.35 & 10.5 & .00067 & .0013 & .00498 & 52.0 & .065 \\
\bottomrule
\end{tabular}
\end{table}

HSIC attains $p=1/201$ at every level of the original MNIST seed-0 sweep and
for every CIFAR-10 seed.  This
outcome is read alongside effect sizes: the three-seed CIFAR-10 $.35$ probe is
only $10.5\%$ and does not support practically recoverable label information
even though the kernel test still detects dependence.  Raw floor-hit is 100\%
at all CIFAR levels;
the experiment manipulates effective noise rather than claiming that the
variance head stops collapsing.

\begin{figure}[ht]
\centering
\begin{minipage}{0.49\textwidth}
\includegraphics[width=\linewidth]{entropy_sweep_mnist.png}\\[-0.4em]
\centering\small (a) MNIST
\end{minipage}\hfill
\begin{minipage}{0.49\textwidth}
\includegraphics[width=\linewidth]{entropy_sweep_cifar10.png}\\[-0.4em]
\centering\small (b) CIFAR-10
\end{minipage}
\caption{Leakage, marginal/conditional discrepancy, and independent Gen-ACC
as effective posterior entropy increases.  Curves are single-seed controlled
interventions, not confidence intervals over training.}
\label{fig:entropy-full}
\end{figure}
\FloatBarrier

\section{Native Cross-Model Details}
\label{app:cross-model}

The fixed population contains 40,000 paired training examples and 10,000
paired test examples.  Each canonical domain audit uses 1,000 balanced test
examples.  The independent Rotated-MNIST evaluator is trained only on real
images and reaches 96.05\% test accuracy.  All models use their final
100-epoch checkpoint for seeds 0--2.

\begin{table}[ht]
\caption{Per-seed native cross-model results, macro-averaged over the two
domains.  Probe, content, and retention values are percentages.}
\label{tab:cross-model-seeds}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrrr}
\toprule
Family & Seed & Global & Cond. & Logit & RBF & HSIC domains & Content & Retention \\
\midrule
F-CS-WAE & 0 & .003287 & .032431 & 94.00 & 97.75 & 2/2 reject & 99.25 & 12.55 \\
F-CS-WAE & 1 & .003209 & .030989 & 92.50 & 96.25 & 2/2 reject & 99.50 & 11.10 \\
F-CS-WAE & 2 & .003481 & .033365 & 89.25 & 96.00 & 2/2 reject & 99.50 & 13.45 \\
\midrule
DRIT & 0 & .259014 & .272479 & 25.00 & 25.50 & 2/2 reject & 87.50 & 9.80 \\
DRIT & 1 & .261287 & .274362 & 29.50 & 23.25 & 2/2 reject & 90.00 & 10.00 \\
DRIT & 2 & .245482 & .270487 & 48.75 & 48.75 & 2/2 reject & 87.25 & 9.80 \\
\midrule
DIVA & 0 & .0000096 & .000268 & 11.50 & 12.50 & .0746, .0398 & 96.00 & 90.25 \\
DIVA & 1 & -.0000452 & .0000288 & 11.00 & 11.25 & .2687, .0249 & 97.50 & 89.50 \\
DIVA & 2 & -.0000181 & .000231 & 9.50 & 11.75 & .1194, .7264 & 96.50 & 88.60 \\
\bottomrule
\end{tabular}}
\end{table}

All F-CS-WAE and DRIT domain-level HSIC values equal $1/201$.  For DIVA, Holm
step-down correction over its six raw tests leaves no rejection.  DIVA's six
global-MMD tests also do not reject.  Its posterior entropy is approximately
1.36--1.38 nats/dimension; F-CS-WAE is approximately -3.58 and DRIT ranges
from -1.88 to -1.57.  The cross-model result therefore does not by itself
remove posterior entropy as a confound for F-CS-WAE; that role belongs to
Appendix~\ref{app:entropy}.

These are the diagnostics whose statistical object transfers without changing
the native sampler.  The direct term-3 and joint-replacement tests below are
implemented for F-CS-WAE only; the table therefore supports cross-family
discrimination by the shared subset, not a complete-contract claim for DRIT
or DIVA.

\section{Post-Specified Complete-Contract Diagnostic}
\label{app:complete-contract}

This diagnostic was specified after the initial checkpoint audit and is
therefore treated as post-specified.  It does not retrain or select
checkpoints: it loads the fixed
F-CS-WAE seeds $\{0,1,2\}$ and the same balanced Rotated-MNIST test
populations used in Appendix~\ref{app:cross-model}.  Each domain supplies
100 posterior samples per digit.  For every digit, a separate RNG stream draws
100 samples from the checkpoint's Spherical Cauchy
$p(\zc\mid y=k)$ and 100 styles from $\mathcal N(0,I)$.

The term-3 test compares posterior and prior content with the chordal
multiscale RBF kernel $\sigma\in\{.1,.3,.5,1,2\}$.  The complete-contract test
compares encoded pairs with independently sampled prior pairs using the
product of that content kernel and the Euclidean style kernel with
$\sigma\in\{.5,1,2,5,10,20,50\}$.  Each reported domain statistic uses
$\sum_k\omega_k\widehat{\MMD}_k^2$ with empirical
$\omega_k=n_k/\sum_jn_j$.  All audited domains contain 100 examples per digit,
so this is numerically the mean of ten classwise unbiased MMD$^2$ statistics.
Its 499 null
replicates independently permute the two sample-bank labels inside each
class before aggregation; plus-one calibration gives minimum $p=1/500$.
For each diagnostic, classwise decisions are corrected by Holm's method
separately within each of the six 10-digit seed--domain families.

\begin{table}[ht]
\caption{Held-out complete-contract diagnostics.  All four aggregate tests
within each row (two domains $\times$ two tests) attain $p=.002$.  Holm
columns sum decisions from the two separate 10-digit domain families.}
\label{tab:complete-contract}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{rrrrrrr}
\toprule
Seed & $T_3^{-30}$ & $T_3^{+30}$ & $T_{\rm joint}^{-30}$ & $T_{\rm joint}^{+30}$ & $T_3$ Holm & Joint Holm \\
\midrule
0 & .004208 & .005682 & .016960 & .017481 & 16/20 & 20/20 \\
1 & .006066 & .013177 & .017694 & .022280 & 20/20 & 20/20 \\
2 & .008142 & .011161 & .020059 & .022097 & 20/20 & 20/20 \\
\bottomrule
\end{tabular}
\end{table}

Across all six seed--domain audits, term-3 MMD$^2$ is
$.00807\pm.00347$ and direct-joint MMD$^2$ is $.01943\pm.00239$ (mean
$\pm$ sample standard deviation).  Every aggregate test rejects.  A single
Bonferroni family over the 12 aggregate tests maps each raw $p=.002$ to
$p_{\mathrm{adj}}=.024$, so all 12 conclusions remain.  At the localized
level, 56/60 term-3 and 60/60 direct-joint digit tests reject after Holm
correction within each of the six 10-digit families for the corresponding
diagnostic; these totals are not the result of one pooled 60-test correction.
The direct result is an omnibus contract check, while the term-3
result shows that content-prior mismatch contributes beyond condition leakage
and within-condition dependence.  The complete JSON, per-class null summaries,
checkpoint/split hashes, and SHA-256 sidecar are stored under
\path{runs_diag/complete_contract_diagnostics}; the result JSON hash is
\texttt{aacbfd1456b02539...3c229838}.

\section{Shapes3D Known-Factor Audit}
\label{app:shapes3d}

Shapes3D provides 480,000 $64\times64$ images containing every combination of
10 floor hues, 10 wall hues, 10 object hues, 8 scales, 4 shapes, and 15
orientations.  The fixed shape-stratified IID split contains 120,000 training,
20,000 validation, and 20,000 test images.  F-CS-WAE and the learned C/S VAE
use seeds $\{0,1,2\}$; the observed-shape oracle has completed three-seed
retention and factor-test follow-ups.  Every model has
64 content and 64 style dimensions and is audited at epoch 100 on 4,096
examples (1,024 per shape).  Global MMD uses 2,048 examples and 500
permutations; factor HSIC uses
up to 1,024 examples and 200 permutations.  Holm correction is applied
separately within each latent view.

\begin{table}[ht]
\caption{Completed three-seed Shapes3D replication.  Entries are rounded
means over seeds $\{0,1,2\}$.  The observed-shape oracle remains a seed-0
auxiliary control and is reported in the main paper.}
\label{tab:shapes3d-aggregate}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrr}
\toprule
Model & $z_s\!\to$shape & $z_c\!\to$style & Cond. shape MMD & Prior shape & Swap shape & Rec. L1 \\
\midrule
F-CS-WAE & 99.0 & 87.0 & .0085 & 89.0 & 88.0 & .0023 \\
Learned C/S VAE & 25.5 & 9.5 & .00004 & 99.7 & 99.8 & .0029 \\
\bottomrule
\end{tabular}}
\end{table}

The completed observed-shape follow-up places $z_c\!\to$style in $9$--$13\%$,
prior and swap shape retention in $99.5$--$100\%$, and raw style--shape HSIC
$p$-values in $.30$--$.80$.  Across seeds 1 and 2, the corrected pattern also
repeats the seed-0 result: F-CS-WAE rejects all six factors in both views; the
learned C/S VAE rejects only shape in content and the five nuisance factors in
style; neither control rejects style--shape dependence after Holm correction.
These are completed, manifest-verified follow-up results.  The detailed table
and plots below display the original seed-0 measurements where the retained
aggregate export does not contain a seedwise numeric statistic; the corrected
three-seed decisions and observed ranges are reported above without
reconstructing unreported per-seed values.

\begin{figure}[ht]
\centering
\begin{minipage}{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{shapes3d_fcswae_factor_loading.png}\\[-0.5em]
\small (a) F-CS-WAE
\end{minipage}\hfill
\begin{minipage}{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{shapes3d_content_style_vae_factor_loading.png}\\[-0.5em]
\small (b) learned content--style VAE
\end{minipage}
\caption{Held-out factor loading on Shapes3D stochastic latents.  F-CS-WAE
stores nearly every factor in both codes; the learned VAE places shape in
content and nuisance factors in style.  Accuracies are descriptive and are
paired with the corrected dependence tests reported in the text.}
\label{fig:shapes3d-loading}
\end{figure}

\begin{table}[ht]
\caption{Seed-0 held-out logistic factor probes on stochastic latent samples.
Columns follow dataset order.  The intended pattern is content high only for
shape and style low only for shape.}
\label{tab:shapes3d-probes-full}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lllrrrrrr}
\toprule
Model & View & Floor & Wall & Object & Scale & Shape & Orient. \\
\midrule
F-CS-WAE & content & 99.76 & 99.88 & 80.98 & 68.05 & 100.00 & 89.88 \\
          & style   & 100.00 & 100.00 & 100.00 & 95.61 & 100.00 & 100.00 \\
\midrule
Observed-shape cVAE & content & 9.76 & 9.51 & 9.02 & 14.27 & 100.00 & 8.29 \\
                    & style   & 100.00 & 99.63 & 99.02 & 45.49 & 25.98 & 39.76 \\
\midrule
Learned C/S VAE & content & 8.90 & 11.22 & 8.90 & 11.95 & 100.00 & 5.85 \\
                & style   & 84.02 & 91.46 & 88.29 & 44.15 & 25.73 & 42.93 \\
\bottomrule
\end{tabular}}
\end{table}

After Holm correction, F-CS-WAE rejects dependence for all six factors in
both views.  Each control rejects only shape in content and only the five
nuisance factors in style.  The corresponding shape HSIC raw $p$-values in
style are .597 for the observed-shape cVAE and .826 for the learned model.

\begin{table}[ht]
\caption{Seed-0 independent factor-evaluator decoder checks.  Posterior swaps keep
content from the source and style from a different-shape donor; ``target'' is
source shape for shape and donor value for nuisance factors.}
\label{tab:shapes3d-decoder-full}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrrr}
\toprule
Model & Floor & Wall & Object & Scale & Orient. & Swap shape & Prior shape \\
\midrule
F-CS-WAE & 100.00 & 99.90 & 99.95 & 80.93 & 100.00 & 87.74 & 89.43 \\
Observed-shape cVAE & 99.98 & 100.00 & 99.80 & 95.02 & 99.93 & 100.00 & 99.88 \\
Learned C/S VAE & 99.98 & 99.93 & 99.85 & 99.73 & 99.93 & 100.00 & 99.78 \\
\bottomrule
\end{tabular}}
\end{table}

All three models pass the frozen competence checks: independent factor
evaluator, content-shape probe, reconstruction L1 below .12, reconstruction
shape accuracy above .8, and mean reconstructed nuisance-factor accuracy
above .7.  The observed-shape cVAE supplies content directly from the label;
it is an oracle control for the intended split, not a like-for-like
representation-learning competitor.  The learned content--style VAE is the
stronger negative control.

\section{Official SCFlow External-Validity Audit}
\label{app:scflow}

\subsection{Scope gate and claim boundary}

This post-specified audit first applies a source-level scope gate rather than
selecting a model from its numerical result.  DEADiff exposes separate style
and content branches, but its released inference path supplies the same
reference image to both; independently assigning donors would therefore be our
intervention rather than its declared sampling law.  It is excluded.  The
official SCFlow implementation at commit
\texttt{7c21af7dc3b3cfaff49876337b77b3aae6cfcdf5} passes the gate: forward
inference accepts distinct content and style images, concatenates their CLIP
embeddings, and training constructs the corresponding cross-image source.
The released module also exposes forward merge and reverse endpoint recovery.
We download the official SCFlow checkpoint and test embeddings, but not the
separate UnCLIP decoder.  Claims are consequently restricted to compatibility,
transport, and retrieval in the released synthetic CLIP space; they do not
cover decoded-image fidelity or diversity.

\subsection{Frozen population and tests}

The official test split contains a complete $51$-style by $3{,}000$-content
grid of 768-dimensional CLIP embeddings.  For each evaluation-subset seed we
select 20 contents and 20 styles, yielding 400 desired combinations.  For each
target $(c,s)$, the content donor has content $c$ and a randomly selected
different style, while the style donor has style $s$ and a randomly selected
different content.  These donors instantiate the source law used by the
official forward merger.  Evaluation-subset seeds change only this held-out
grid and donor assignment; they are not independently trained model seeds.

We retain the official inference default of one Euler step.  Forward inference
maps the concatenated donors to two target endpoints; their cosine agreement is
above .994 in every subset, so we average them for target comparisons.  Reverse
inference duplicates the target embedding and recovers a content/style endpoint
pair.  Denote the recovered-pair law by $Q^{\rm end}$ and the law of the
independently selected content/style donor pair by $P^{\rm donor}$.  We compare
$Q^{\rm end}$ with $P^{\rm donor}$ using unbiased multiscale MMD and unbiased energy distance, each with 199
balanced pooled-label permutations.  The same two tests separately assess both
marginals.  Delta-label HSIC tests whether recovered content varies with target
style and recovered style with target content; Holm correction covers both
directions across all three subsets.  Transport reports forward target cosine
and forward--reverse cycle cosine.  Competence uses exact content, style, and
joint nearest-neighbor retrieval against all 153,000 official test embeddings;
simple donor averaging is the frozen baseline.

\begin{table}[ht]
\caption{Official SCFlow audit over three evaluation-subset seeds.  Each row
contains 400 combinations.  Joint MMD and energy have $p=1/200$ in every row;
both marginal tests do also.  Cosine and joint retrieval show
SCFlow/donor-mean baseline; cycle shows content/style.}
\label{tab:scflow-official}
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{rrrrrrr}
\toprule
Subset seed & Joint MMD & Joint energy & Target cosine & Cycle cosine & Joint ret. (\%) & C/S ret. (\%) \\
\midrule
2027 & .07002 & 2.8207 & $.859/.765$ & $.251/.315$ & $9.75/.00$ & $17.75/71.50$ \\
2028 & .06585 & 2.6639 & $.886/.792$ & $.249/.341$ & $12.50/.25$ & $18.75/81.00$ \\
2029 & .07158 & 2.8207 & $.883/.787$ & $.236/.329$ & $15.50/.00$ & $19.75/79.50$ \\
\midrule
Mean $\pm$ SD & --- & --- & $.876/.782$ & $.245/.328$ & $12.58/.08$ & $18.75/77.33$ \\
\bottomrule
\end{tabular}
\end{table}

MMD and energy agree: joint, content-marginal, and style-marginal equality are
rejected in all three subsets.  Cross-factor dependence does not explain this
failure.  Only one of six directional HSIC tests has raw $p<.05$ ($.035$), and
none survives the familywise Holm correction.  At the same time, the forward
merger improves target cosine by $.094\!\pm\!.001$ and joint retrieval from
$.08\!\pm\!.14\%$ to $12.58\!\pm\!2.88\%$.  Style retrieval is stronger than
content retrieval, while the low cycle cosines show that useful forward
composition does not entail accurate inverse transport.  This is the
three-level distinction in one official checkpoint: endpoint-law compatibility
fails, forward transport is useful but not invertibly consistent, and
CLIP-space competence improves over a simple baseline.  It is not a claim that
SCFlow instantiates the encoder--prior equality in Theorem~\ref{thm:decomp},
that SCFlow is generically ineffective, or that exact equality is required for
every downstream use.

The main summary is stored at
\path{runs_modern/scflow_audit/summary.json} (SHA-256
\texttt{afc6330fc69d82c...4e3be60e02cbe41}).  It embeds hashes for all three
seed artifacts.  The checkpoint hash is
\texttt{51f3ed03013c0ed...37d8a265902054}; the official test-split hash is
\texttt{b6d6b1338a2382ad...c514b5f0bb5c0c8}.  The separate feasibility gate is
\path{runs_modern/scflow_feasibility/gate.json}.  A 100-combination pilot was
used only to debug technical execution and is excluded from every aggregate.

\section{Clause-Aligned FACT Intervention}
\label{app:fact}

\subsection{Objective and optimization}

The intervention study uses the same MNIST architecture as the case study but a
40-epoch decision protocol with batch size 320.  Phase boundaries are
$(5,12,26,40)$; scheduled weights are frozen at zero-indexed epoch 34, and
\FACT{} becomes active at epoch 12.  When active, the legacy semantic-class
and per-class style penalties are replaced by the null-calibrated style,
content, and sampled dependence constraints described in
the following equation.  For classwise statistic $T_k$,
\begin{equation}
C_T=\frac1{|\mathcal K|}\sum_{k\in\mathcal K}
\left[T_k-\frac1B\sum_{b=1}^{B}T^0_{kb}\right]_+,\qquad B=4,
\label{eq:fact-null}
\end{equation}
where $T^0$ uses matched prior--prior banks or within-class permutations.
Reconstruction, aggregate content MMD, global style MMD, and semantic
classification remain active.  The direct joint statistic is never optimized.

Writing the three core positive-excess constraints as
$(C_s,C_c,C_d)$, sampled style--label HSIC as $C_y$, posterior-mean
conditional HSIC as $C_\mu$, and adversarial uniform confusion as $C_a$, the
active objective is
\begin{equation}
\mathcal L_{\FACT}=\mathcal L_{\mathrm{base}}
+\lambda_s C_s+\lambda_c C_c+\lambda_d C_d
+100C_y+0.1C_a+4C_\mu .
\end{equation}
The adversary is a $128,64$ ReLU MLP, updated once per encoder minibatch with
Adam learning rate $10^{-3}$ and weight decay $10^{-4}$.  Its classifier step
uses detached sampled style; the encoder step freezes adversary parameters and
minimizes $\KL(U\|q_\phi(y\mid\zs))$.  Both sampled and mean conditional-HSIC
paths stop gradients into content.

The core multipliers start at one and are projected into $[0,10]$.  Once per
epoch, the update for constraint $j$ is
\begin{equation}
\lambda_j\leftarrow\Pi_{[0,10]}\left(
\lambda_j+0.1\,\overline C_j/r_j\right),
\end{equation}
with reference scales $(r_s,r_c,r_d)=(.002,.0015,2.5\!\times\!10^{-5})$ and
an absolute per-epoch step cap of .25.  All tolerances are zero after matched
null subtraction; every null floor averages four independent draws.

For $C_\mu$, content means are L2-normalized and style means are standardized
featurewise inside each represented class.  The content kernel averages
spherical chordal RBFs with bandwidths $(.1,.3,.5,1,2)$; the style kernel
averages Euclidean RBFs with bandwidths $(.5,1,2,5,10,20,50)$.  Both Gram
matrices are centered, their elementwise product is divided by
$(n_k-1)^2$, and the null permutes the standardized style Gram matrix within
class.  This is the same geometry as the held-out posterior-mean diagnostic.

\subsection{Calibration, development path, and frozen gates}

The retained development checkpoints form the cumulative path in
Table~\ref{tab:fact-components}.  This is a sequential seed-0 development
ablation: each row adds or corrects a component selected after the preceding
row, so it explains the construction but is not a factorial causal estimate.

\begin{table}[ht]
\caption{Cumulative component path on development training seed 0 and audit
RNG seed 0.  Reductions and MLP are percentages; HSIC is relative to control.}
\label{tab:fact-components}
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrr}
\toprule
Cumulative configuration & Joint $\downarrow$ & Cond. MMD $\downarrow$ & MLP & HSIC/control \\
\midrule
Schedule control & 0.00 & 0.00 & 72.68 & 1.000 \\
Core null-calibrated constraints & 8.68 & 7.09 & 72.93 & 1.358 \\
$+$ normalized estimator/dual correction & 21.62 & 30.85 & 56.59 & 1.437 \\
$+$ sampled label HSIC & 30.73 & 21.53 & 48.29 & 1.572 \\
$+$ nonlinear adversary & 30.33 & 28.97 & 11.46 & 1.069 \\
$+$ posterior-mean HSIC ($w_\mu=4$) & 39.20 & 34.12 & 10.98 & .487 \\
\bottomrule
\end{tabular}
\end{table}

Calibration uses 20 MNIST minibatches from the frozen label-adversary
checkpoint, with batch size 320 and four null draws.  Mean raw conditional
HSIC is $.004647\pm.000240$, its matched-null floor is
$.003996\pm.000328$, and positive excess is $.000651\pm.000112$.  Target
initial objective contributions of roughly $.001,.003,$ and $.01$ yield the
pre-specified weights $\{2,5,20\}$.  Weight 4 is a final interpolation between
the underpowered weight 2 and reconstruction-limited weight 5; it is selected
on model seed 0 and then frozen before model seeds 1 and 2.

The seven gates are: semantic accuracy at least .985; reconstruction MAE no
more than 1.10 times the schedule control; direct joint and conditional style
MMD reductions of at least 20\%; sampled-style MLP and RBF-SVM accuracies no
greater than .40; and posterior-mean conditional-HSIC ratio no greater than
1.15.  Evaluation seeds alter posterior/audit RNG and probe splits for the
same model checkpoint; they are not independent training seeds.

\begin{table}[ht]
\caption{Seed-0 development path, averaged over evaluation seeds 0--2.
Reductions, reconstruction change, and MLP are percentages.  ``Pass'' counts
evaluation seeds satisfying all seven gates.}
\label{tab:fact-development}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrrrrr}
\toprule
Run & $w_\mu$ & Rec. $\Delta$ & Joint $\downarrow$ & Cond. $\downarrow$ & MLP & HSIC/control & Pass \\
\midrule
Label adversary & 0 & 2.19 & 33.59 & 25.67 & 11.46 & 1.181 & 1/3 \\
Mean HSIC & 2 & 3.45 & 35.26 & 26.72 & 11.06 & 1.196 & 1/3 \\
Mean HSIC & 4 & 5.96 & 40.93 & 32.96 & 10.89 & .476 & 3/3 \\
Mean HSIC & 5 & 10.49 & 44.27 & 29.04 & 12.85 & .966 & 0/3 \\
Mean HSIC & 20 & 11.94 & 40.53 & 4.29 & 11.38 & .365 & 0/3 \\
\bottomrule
\end{tabular}
\end{table}

Weight 20 demonstrates that the new constraint can strongly lower its held-out
target, but conditional style matching and reconstruction degrade.  Weight 5
misses only the 10\% reconstruction gate in all evaluation seeds.  The
interpolated weight 4 passes every gate for every evaluation seed and is the
only configuration advanced to independent training-seed confirmation.

\subsection{Development-era matched controls}

Training seed 0 is the development model.  Seeds 1 and 2 are independently
trained initial confirmations after all arguments are locked.  We additionally
train a schedule control for each of seeds 1 and 2, yielding matched
control/\FACT{} pairs for training seeds 0--2.  All six checkpoints use audit
RNG seed 0.  Table~\ref{tab:fact-seeds} reports paired effects rather than
differences from one fixed control.

\begin{table}[ht]
\caption{Matched control/\FACT{} confirmation.  Semantic $\Delta$ is in
percentage points; reconstruction and reductions are relative percentages.
MLP shows control $\rightarrow$ \FACT{}.  HSIC $\downarrow<0$ denotes a
worsening.  Every training seed is retained.}
\label{tab:fact-seeds}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{rrrrrrrr}
\toprule
Training seed & Semantic $\Delta$ & Rec. $\Delta$ & Joint $\downarrow$ & Cond. $\downarrow$ & MLP control$\to$FACT & HSIC $\downarrow$ & Gates \\
\midrule
0 & $-.45$ & $+5.94$ & 39.20 & 34.12 & $72.68\to10.98$ & 51.34 & 7/7 \\
1 & $-.50$ & $+12.35$ & 35.09 & 13.92 & $80.49\to11.46$ & $-30.89$ & 4/7 \\
2 & $+.10$ & $+1.26$ & 42.37 & 28.39 & $77.56\to10.98$ & 36.59 & 7/7 \\
\midrule
Mean & $-.28$ & $+6.52$ & 38.89 & 25.48 & $76.91\to11.14$ & 19.01 & 2/3 seeds \\
SD & .33 & 5.57 & 3.65 & 10.41 & --- & 43.84 & --- \\
\bottomrule
\end{tabular}}
\end{table}

Absolute joint MMD is $.002368\pm.000160$ for controls and
$.001449\pm.000166$ for \FACT{}; absolute classwise style--prior MMD is
$.002567\pm.000153$ and $.001902\pm.000148$, respectively.  Every aggregate
joint test rejects at $p=.005$ before and after the intervention, so the paired
reduction does not establish exact compatibility.  Seed 1 also fails the
reconstruction, conditional-style-MMD, and posterior-mean-HSIC gates.  The
nonlinear leakage effect is stable, whereas mean conditional dependence is not.

\subsection{Preliminary post-freeze training seeds}

After the entire development and initial-confirmation sequence, training seeds
3--5 are run from epoch 1 without changing any argument.  These are clean
post-freeze checks and are not pooled with the matched seed 0--2 analysis.
Their pre-specified gates retain the frozen schedule
control seed 0 as the reference, exactly as declared before the runs.

\begin{table}[ht]
\caption{Complete post-freeze confirmation.  Reduction and MLP columns are
percentages; HSIC is a ratio to the frozen schedule-control seed 0.}
\label{tab:fact-postfreeze}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{rrrrrrrr}
\toprule
Training seed & Semantic & Rec. MAE & Joint $\downarrow$ & Cond. $\downarrow$ & MLP & HSIC/control & Gates \\
\midrule
3 & 98.80 & .020477 & 22.53 & 20.36 & 11.46 & .373 & 7/7 \\
4 & 99.20 & .021081 & 18.02 & 12.39 & 11.46 & .583 & 5/7 \\
5 & 98.95 & .021055 & 29.43 & 25.83 & 11.46 & .624 & 7/7 \\
\midrule
Mean & 98.98 & .020871 & 23.33 & 19.53 & 11.46 & .527 & 2/3 seeds \\
SD & .20 & .000342 & 5.75 & 6.75 & .00 & .135 & --- \\
\bottomrule
\end{tabular}
\end{table}

Seed 4 misses only the two 20\% distributional-reduction gates.  It passes
semantic competence, reconstruction, both nonlinear probes, and the
posterior-mean-HSIC gate.  These preliminary outcomes motivate reporting the
matched seed-level rows, including their adverse results, as the intervention
evidence in the main paper.

\subsection{Post-specified kernel-free robustness}
\label{app:fact-kernel-robustness}

We rerun the frozen sample-bank audit with unbiased energy distance in the
declared latent coordinates and replace conditional HSIC by distance
correlation.  Content uses chordal distance on the unit sphere; style uses the
native unit-Gaussian-prior scale; the joint concatenates
$\zc/\sqrt{2}$ and $\zs/\sqrt{2d_s}$.  Each statistic uses 200 samples per
class per bank and 199 matched permutations.  This analysis was specified
after the MMD/HSIC results and is not used for model selection.

\begin{table}[ht]
\caption{Kernel-free robustness.  Reductions are percentages relative to the
matched control (seeds 0--2) or frozen control seed 0 (seeds 3--5); positive
values favor \FACT{}.  ``Agree'' is MMD--energy equality-decision agreement
over 33 aggregate/classwise comparisons.}
\label{tab:fact-kernel-robustness}
\centering
\scriptsize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{llrrrr}
\toprule
Role & Seed & Joint MMD $\downarrow$ & Joint energy $\downarrow$ & Cond. dCor $\downarrow$ & Agree \\
\midrule
Matched & 0 & 39.20 & $-204.07$ & 38.46 & 96.97 \\
Matched & 1 & 35.09 & 48.49 & 7.11 & 100.00 \\
Matched & 2 & 42.37 & 55.37 & 5.73 & 100.00 \\
\midrule
Post-freeze & 3 & 22.53 & 31.13 & 24.98 & 96.97 \\
Post-freeze & 4 & 18.02 & $-15.77$ & 37.90 & 96.97 \\
Post-freeze & 5 & 29.43 & $-108.70$ & 32.44 & 100.00 \\
\bottomrule
\end{tabular}
\end{table}

Both dependence estimators reject within-class independence for every model,
while HSIC and distance correlation decrease for all three matched pairs and
all three preliminary post-freeze models.  MMD and energy agree on 99.0\% of
the equality-test decisions, showing that contract rejection is not specific
to the RBF kernel.  This decision agreement must not be read as agreement about
intervention magnitude: joint energy improves in only two of three matched
pairs and one of three post-freeze comparisons, with large adverse values for
matched seed 0 and post-freeze seed 5.  The main paper therefore limits the
intervention claim to the selected MMD diagnostic and decoder attribution.

\subsection{Paired decoder interventions}

The independent pixel classifier evaluates deterministic posterior-mean swaps
and 10,000 prior samples per checkpoint.  The class-conditional style sampler
is a post-hoc diagnostic: it deliberately exploits label-bearing style and is
not the declared factorized generation law.

\begin{table}[ht]
\caption{Paired decoder effects.  Swap columns are \FACT{} minus control in
percentage points.  Gen-ACC shows control $\rightarrow$ \FACT{}.}
\label{tab:fact-decoder}
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{rrrrr}
\toprule
Training seed & Swap-content $\Delta$ & Swap-style $\Delta$ & Global Gen-ACC & Class-style Gen-ACC \\
\midrule
0 & $+21.00$ & $-25.70$ & $38.52\to26.85$ & $99.74\to11.18$ \\
1 & $+34.70$ & $-35.22$ & $38.57\to31.92$ & $99.96\to96.93$ \\
2 & $+20.53$ & $-26.33$ & $35.37\to23.90$ & $99.68\to28.45$ \\
\midrule
Mean $\Delta$ & $+25.41$ & $-29.09$ & $-9.93$ & $-54.27$ \\
SD & 8.05 & 5.32 & 2.84 & 45.22 \\
\bottomrule
\end{tabular}
\end{table}

All swap pairs move in the intended direction, directly showing less decoder
reliance on style.  All global-prior pairs lose Gen-ACC.  FACT therefore
improves an audited latent relation and a posterior-swap mechanism, but does not
restore the intended prior-generation behavior.  The high variance of the
class-style diagnostic further shows that its shortcut is not a stable target.

\subsection{Intervention artifacts}

Calibration is stored at
\path{runs_diag/fact/label_adversary_mnist_40e/mean_hsic_calibration.json}
(SHA-256 \texttt{776540a7ebed3a3aa...42eeb23}).  The evaluation-seed summary
is under \path{runs_diag/fact/mean_hsic_mnist_40e/bridge_w4}
(SHA-256 \texttt{65698f6aaa346409...6bbf79}); the independent model-seed
summary is under
\path{runs_diag/fact/mean_hsic_mnist_40e/model_seed_confirmation_w4}
(SHA-256 \texttt{fe69797ae30deba7...b704705}).  The matched-control,
post-freeze, and decoder-intervention summary is
\path{runs_diag/fact/mean_hsic_mnist_40e/submission_followup_v1/summary.json}
(SHA-256 \texttt{6b1e693862820af9...36f73d0cb44}); its manifest hash is
\texttt{f5eea2651fc76aad...f7a1d5556b77}.  It records source hashes, exact
commands, checkpoint hashes, training-seed roles, audit RNG seed, and every
adverse result.  Checkpoints are saved every epoch with RNG, optimizer,
scheduler, dual, and adversary state; resume rejects configuration mismatches.
The implementation is tested by 27 focused loss, gradient, dual-update, phase,
adversary, and checkpoint round-trip tests.
The kernel-free audit is stored at
\path{runs_diag/kernel_robustness/summary.json} (SHA-256
\texttt{11d2e5358023fa14...159fca4f}); it records both protocol-input hashes
and every matched/post-freeze value.

\section{Reproducibility and Evidence Boundary}
\label{app:evidence}
\label{app:reproducibility}

The original checkpoint audit uses protocol \texttt{stage0-1.1.0}; synthetic,
entropy, cross-model, and Shapes3D audits use \texttt{stage0-1.2.0}; the
kernel-free extension uses \texttt{stage0-1.3.0}; the official SCFlow audit
uses \texttt{scflow-contract-audit-1.1.0}.  Table~\ref{tab:artifact-roots}
collects the primary roots rather than scattering paths through the main text.

\begin{table}[ht]
\caption{Primary evidence roots in the anonymous artifact snapshot.}
\label{tab:artifact-roots}
\centering
\footnotesize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lp{0.68\textwidth}}
\toprule
Evidence family & Relative artifact root \\
\midrule
Exact marginal & \path{runs_diag/synthetic_exact_marginal_paper} \\
Entropy & \path{runs_f/entropy_sweep} \\
Cross-family & \path{runs_cross_model/native_v1} \\
Shapes3D & \path{runs_shapes3d/factor_audit_v1} \\
Complete contract & \path{runs_diag/complete_contract_diagnostics} \\
\FACT{} intervention & \path{runs_diag/fact/mean_hsic_mnist_40e} \\
Kernel-free checks & \path{runs_diag/kernel_robustness} \\
Official SCFlow & \path{runs_modern/scflow_audit} \\
\bottomrule
\end{tabular}
\end{table}
Rounded follow-up values are consolidated in
\path{fcswae/reported_followup_aggregates_v16.json}.
Its SHA-256 sidecar fixes the aggregate export at
\texttt{83e4c9038f944782...60aa665ca1d63d4}.
Each final JSON has a SHA-256 sidecar or embeds the criteria, checkpoint,
split, evaluator, source-file, and repository hashes.  Synthetic criteria are
frozen in \texttt{acceptance\_criteria.json}, with the composite decision in
\texttt{acceptance\_result.json}.  Entropy summaries record the frozen CIFAR
promotion decision.  Cross-model reporting is derived from nine native
checkpoint audits without using the legacy proxy artifacts.  Shapes3D suite
completion records all three models as complete and competence-passing.  The
complete-contract artifact records its post-specified status, independent sample
banks, kernel ladders, permutation counts, and checkpoint/split hashes.
The completed sample-size sweep and additional three-seed MNIST,
Fashion-MNIST, CIFAR-10 entropy, and Shapes3D runs are reported here as
rounded aggregate exports.  Each follow-up was run under the frozen audit
protocol and checked against its manifest and SHA-256 sidecar before
aggregation; the corresponding run artifacts are retained in the audit
archive separately from this source repository.  We preserve the verified
mean/SD or observed-range semantics and do not reconstruct unreported
standard deviations or aggregate $p$-values from cross-experiment CV bands.
Seedwise decision claims are stated only when the calibrated decision agrees
across every recorded run.

Some historical artifacts record a dirty worktree; that provenance is retained
rather than relabeled.  The anonymous snapshot includes the exact critical
source files and \path{fcswae/v26_evidence_manifest.json}, whose generated
sidecar binds them to the traceable headline summaries.  The present scope is 50
synthetic replications at the primary
$n=2{,}048$ setting, four sample sizes at $\kappa=16$, three training seeds per
native cross-model family, three-seed original-image headlines, three
CIFAR-10 entropy seeds, three F-CS-WAE/learned-VAE Shapes3D seeds, three
MNIST entropy seeds, three observed-shape-oracle follow-up seeds, three matched
MNIST control/\FACT{} pairs, and three additional post-freeze \FACT{} training
seeds.  The official SCFlow result uses one released checkpoint and three
seeded 400-combination evaluation subsets; these are evaluation seeds, not
model seeds.  Aggregates without locally available seed-level artifacts are
excluded from this version.  We make no uncertainty claim beyond those
boundaries.  Submission
source, rendered author blocks, and supplementary metadata must remain
anonymous during review.
